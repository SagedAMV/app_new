# تقرير إعادة البناء والتحسينات — من `app_university_kotlin` إلى `app_new` (UniHub)

## ما الذي فُهم من التطبيق المرجعي؟

بعد قراءة معمّقة لكل ملفات المستودع المرجعي (`UniManager`) تبيّن أنه تطبيق إدارة جامعية يتكون من:

- **6 كيانات**: مجلدات (متداخلة)، ملفات (تُستورد عبر SAF وتُنسخ للتخزين الداخلي)، محاضرات (جدول أسبوعي)، مهام، ملاحظات، امتحانات.
- **المنطق**: قاعدة Room مع هجرات، مستودع واحد عام، **ViewModel واحد ضخم (~600 سطر)** يخدم كل الشاشات، إشعارات عبر WorkManager، نسخ احتياطي JSON، وسمة قابلة للتبديل عبر DataStore.
- **الواجهات**: رئيسية مليئة بالأنيميشن، شاشة "موحّدة" بأربعة تبويبات (~1100 سطر في ملف واحد)، شاشة ملفات (~1000 سطر)، وشاشة "مجرة" لرسم المجلدات.

## المشاكل التي اكتُشفت في المرجع وحُلّت في البناء الجديد

| # | المشكلة في المرجع | الحل في UniHub |
|---|---|---|
| 1 | `AppViewModel` واحد ضخم يجمع كل عمليات التطبيق: أي شاشة تحمّله يحمل معها كل تدفقاتها حتى غير المستخدمة | ViewModel مستقل صغير لكل شاشة/تبويب، كل شاشة تشتريك فقط بما تعرضه |
| 2 | `AppRepository` كائن واحد عام لكل الكيانات | مستودعات لكل مجال (مجلدات، ملفات، مهام، ملاحظات، امتحانات، جدول، لوحة، صيانة) بمسؤولية واضحة |
| 3 | نصوص سحرية في كل مكان: `"high"/"medium"`، `"نصفي"/"نهائي"`، أيام الأسبوع بالعربية الحرة تُقارن نصياً في SQL | `enum` بأنواع قوية (`TaskPriority`, `ExamType`, `Weekday`, `FileKind`) مع `TypeConverter` — أخطاء الأنواع مستحيلة برمجياً |
| 4 | تنقّل بمسارات نصية (`"files?folderId={folderId}"`) ومفاتيح وسيطات يدوية | تنقّل آمن الأنواع (Navigation 2.8 + kotlinx.serialization): كائنات مسارات، والمترجم يضمن صحتها |
| 5 | شاشة موحّدة 1100+ سطر تعيد تركيب أربعة أقسام معاً | شاشة مخطط بتبويبات، كل تبويب ملف مستقل بـ ViewModel خاص |
| 6 | منطق نسخ/فتح الملفات مكتوب داخل ملفات الواجهة | `FileStorage` و`FileOpener` معزولان قابلان للاختبار، مع إزالة تكرار الأسماء وحد أقصى للحجم وحذف آمن داخل مجلد التطبيق فقط |
| 7 | أنيميشن مفرط (خلفيات متلألئة، 150 نجمة، تأثيرات مستمرة) يستهلك البطارية ويشتت القراءة | تصميم هادئ ثابت عملياً؛ الحركة الوحيدة المستمرة في المجرة بطيئة (دورة كاملة كل 150 ثانية) |
| 8 | ألوان بنفسجية/زمردية مشبعة تُجهد العين في القراءة الطويلة | لوحة هادئة: أخضر مائل للرمادي (Sage) + محايدات ورقية دافئة، ووضع ليلي رمادي مخضرّ غير أسود قاسٍ |
| 9 | المظهر ثنائي فقط (فاتح/داكن) | ثلاثة أوضاع: تلقائي حسب النظام / فاتح / داكن + خيار ألوان Material You |
| 10 | فرز المهام بنص الأولوية وترتيب `dueDate` الفارغ قد يتصدر القائمة | فرز في SQL مع دفع التواريخ الفارغة لآخر القائمة، وحقول `completedAt` لتتبع الإنجاز |
| 11 | لا تثبيت للملاحظات ولا بحث في ملاحظات | تثبيت الملاحظات (تظهر أولاً) + بحث لحظي في العنوان والمحتوى |
| 12 | الامتحانات تُعرض كلها بلا تمييز زمني | قادمة/منتهية منفصلتان، مع عدّاد أيام ملوّن (أحمر ≤ يومين، كهرماني ≤ أسبوع) |
| 13 | التحقق من المدخلات برموز نمطية متفرقة ورسائل استثناءات عامة | `InputValidator` موحد برسائل عربية واضحة (`InputError` sealed) وفحوصات نطاق وقت للمحاضرات |
| 14 | عدّادات الملفات للمجلدات تُحسب في الذاكرة | استعلام استضمام واحد في قاعدة البيانات (`FolderWithFileCount`) |
| 15 | تواريخ تُحلل في عدة أماكن بلا مرجع واحد | `DateFormats` مركزي بتحليل آمن يعيد `null` بدل الاستثناءات + تسميات ودّية (اليوم/غداً/بعد ٣ أيام/متأخر) |
| 16 | رسائل نجاح/خطأ في حقلين منفصلين بالـ ViewModel العام | `UiMessenger` واحد لكل ViewModel يعرض رسائله في Snackbar الشاشة |

## ما بُني من الصفر (هيكل المشروع الجديد)

```
app/src/main/java/com/unihub/app/
├── MainActivity.kt / UniHubApplication.kt     # نقطة الدخول + قنوات الإشعارات
├── core/
│   ├── common/      # DateFormats، Formatters، UiMessenger
│   ├── prefs/       # ThemePreferenceManager (DataStore) بثلاثة أوضاع
│   └── validation/  # InputValidator الموحّد
├── data/
│   ├── local/       # الكيانات، المحولات، 6 DAOs، قاعدة البيانات، إسقاطات
│   ├── repository/  # 8 مستودعات بمسؤولية لكل مجال
│   ├── storage/     # FileStorage — النسخ الفيزيائي الآمن للملفات
│   └── backup/      # BackupRepository — تصدير/استيراد JSON (إصدار 3)
├── di/              # وحدة Hilt
├── feature/
│   ├── dashboard/   # الرئيسية: لقطة اليوم
│   ├── files/       # تصفح المجلدات، استيراد، فتح/مشاركة
│   ├── planner/     # المهام، الجدول، الملاحظات، الامتحانات (4 أزواج VM+Tab)
│   ├── galaxy/      # المجرّة — كانفاس خفيف + كواكب قابلة للنقر
│   ├── settings/    # المظهر والبيانات
│   └── backup/      # التصدير والاستعادة
├── notifications/   # ReminderScheduler + Worker + القنوات
└── ui/
    ├── components/  # مكونات مشتركة (ألواح، حقول، منتقيات، حالات فارغة)
    ├── navigation/  # مسارات @Serializable + الشريط السفلي + المضيف
    └── theme/       # الألوان الهادئة + السمة + الخطوط
```

## قرارات تقنية مهمة

1. **البدء بقاعدة بيانات الإصدار 1 بلا هجرات موروثة** — المخطط الجديد نظيف (كل الفهارس والمفاتيح الأجنبية معرفة من البداية)، مع `fallbackToDestructiveMigration` لأن التطبيق شخصي غير منشور (موثق في الكود).
2. **الأسماء والأولويات والأنواع تُخزن كأسماء الـ enums** — يسهل قراءة قاعدة البيانات يدوياً ويُبقى التوسع ممكناً.
3. **التذكيرات مرتبطة بدورة حياة السجل**: إنشاء/تعديل/حذف مهمة أو امتحان يجدول أو يلغي أعمال WorkManager تلقائياً داخل المستودع، وبعد الاستيراد تُلغى كل الأعمال وتُجدول القادمة من جديد (لا إشعارات شبحية).
4. **كل شاشة تبني حالتها عبر `combine` من تدفقات القاعدة** — البحث والتصفية لحظيان بلا استعلامات إضافية.

## اختبارات الوحدة

- `InputValidatorTest`: الأسماء، الامتدادات، التواريخ، نطاقات الوقت، التنظيف.
- `DateFormatsTest`: التحليل الآمن، عدّ الأيام، التسميات الودية، تركيب التاريخ والوقت.

## ملاحظات صادقة

- البناء ركّز على **الهيكل والمنطق والتصميم** كما طُلب؛ لم تُضف ميزات جديدة خارج نطاق فكرة التطبيق الأصلي (لا مزامنة سحابية ولا حسابات — التطبيق يبقى محلياً بالكامل).
- شاشة المجرة أُبقيت لأنها من روح التطبيق الأصلي، لكن أعيد بناؤها لتكون أخف وأسهل صيانة.

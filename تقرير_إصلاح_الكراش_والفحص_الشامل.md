# تقرير إصلاح كراش الإقلاع والفحص الشامل

> جلسة مراجعة كاملة للكود (كل ملف في المستودع قُرئ وفُحص يدوياً)، تنفيذًا لما ورد في `تعليمات.md`:
> فهم منطق التطبيق، إيجاد سبب الكراش عند فتح التطبيق وحله جذرياً، فحص الأكواد الخاطئة
> والدوال الميتة، ثم رفع التعديلات للمستودع.

---

## أولاً: الكراش عند فتح التطبيق — السبب الجذري والحل

### فهم مسار الإقلاع

عند فتح التطبيق ينفَّذ هذا المسار قبل ظهور أول إطار:

1. `UniHubApplication.onCreate()` → إنشاء قنوات الإشعارات.
2. حقن Hilt لـ `MainActivity` ثم `installSplashScreen()` و`setContent`.
3. `UniHubApp` يقرأ **تفضيلات المظهر من DataStore** (`ThemeViewModel` ← `ThemePreferenceManager`).
4. `AppNavHost` يبدأ بـ `DashboardScreen` التي تشغّل **8 تدفقات Room** فوراً عبر `DashboardViewModel`.

أي استثناء في الخطوتين 3 أو 4 = إغلاق فوري للتطبيق قبل ظهور أي شاشة.

### السبب الجذري رقم 1: قاعدة بيانات من بِناء سابق بمخطط غير متوافق

التطبيق تطوّر عبر بنات تطوير متعاقبة على نفس الجهاز. إذا وُجد ملف `unihub_db` أنشأته
بِناءة سابقة بمخطط مختلف (هوية مخطط مختلفة)، فإن Room يرمي
`IllegalStateException` عند **أول فتح** — أي كراش فوري عند تشغيل التطبيق.

**النقطة الجوهرية:** `fallbackToDestructiveMigration()` الموجود في `AppModule`
**لا يغطي هذه الحالة إطلاقاً**؛ هو يعمل فقط عند تغيّر رقم الإصدار، بينما عدم تطابق
هوية المخطط مع نفس رقم الإصدار يرمي الاستثناء مباشرة من `RoomOpenHelper.checkIdentity`.

**الحل الجذري** (ملف جديد `data/local/DatabaseSelfHeal.kt` + استدعاؤه في `UniHubApplication`):

- قبل أن يلمس أي مكوّن (Hilt/ViewModel) القاعدة، يُفتح الملف فعلياً عبر نسخة فحص مهملة.
- نجح الفتح → تُغلق نسخة الفحص ويكمل التطبيق طبيعياً (صفر تكلفة على التثبيت الجديد؛
  الفحص يُتخطى كلياً إن لم يوجد ملف).
- فشل الفتح → يُحذف الملف مع ملحقاته (`-wal`/`-shm`) ويُنشئ Room قاعدة نظيفة.
  التطبيق شخصي غير منشور، فالإعادة النظيفة أفضل من حلقة كراش لا نهائية.

### السبب الجذري رقم 2: ملف تفضيلات DataStore تالف

ملف `unihub_settings.preferences_pb` لو تلف (قطع كتابة، عملية قُتلت أثناء الحفظ،
بِناءة أقدم) فإن `dataStore.data` يرمي `CorruptionException`/`IOException` عند أول
قراءة في `ThemeViewModel` — كراش قبل أول إطار.

**الحل** (في `ThemePreferenceManager`):

- القراءة تمر عبر تدفق محصّن (`catch`): عند تلف الملف تُعاد التفضيلات الافتراضية
  ويُحذف الملف التالف لتبدأ القراءة التالية نظيفة — التطبيق يفتح دائماً.
- الكتابة (`setThemeMode`/`setDynamicColor`) محصّنة أيضاً: فشل الحفظ على ملف تالف
  يتبعه حذف الملف ومحاولة واحدة إضافية، والفشل الثاني يُسجَّل في Log بدل إسقاط التطبيق.

### نقطة إضافية في نفس المسار: `Weekdays.fromJava`

كانت تستخدم `first {}` التي ترمي `NoSuchElementException` نظرياً. أصبحت
`firstOrNull ?: SUNDAY` — تحويل آمن لا يرمي استثناء أبداً، اتساقاً مع فلسفة
"التحليل الآمن" في `DateFormats`.

---

## ثانياً: أخطاء منطقية حقيقية اكتُشفت وأُصلحت

### 1. استيراد النسخة الاحتياطية كان يفشل بسبب ترتيب المفاتيح الأجنبية

`folders` تُصدَّر بترتيب `createdAt DESC` — أي أن **الابن قد يُدرج قبل أبيه** عند
الاستيراد، والمفتاح الأجنبي الذاتي على `parentId` (مع تفعيل Room للـ FK) يجعل
SQLite ترفض الإدراج وتُفشل المعاملة كاملة ("فشل الاستيراد").

**الإصلاح** في `BackupRepository`:

- دالة جديدة `orderFoldersForInsertion`: ترتّب المجلدات هرمياً (كل أب قبل أبنائه)
  لأي عمق تداخل.
- المجلدات اليتيمة (تشير إلى أب غير موجود في النسخة) تُهمل بدل إفساد المعاملة.
- الملفات التي تشير إلى مجلدات مستبعدة تُستبعد تلقائياً لنفس السبب.
- عدّاد العناصر المستوردة في رسالة النجاح أصبح يعكس ما استُورد فعلاً.

### 2. عنوان المجلد الحالي لا يتحدث بعد إعادة التسمية

`FilesViewModel.currentFolder` كان قراءة واحدة (`flow { emit(...) }`) — بعد إعادة
تسمية المجلد الحالي تبقى الترويسة بالاسم القديم حتى مغادرة الشاشة.

**الإصلاح:** التدفق أصبح مرتبطاً بعداد `folderVersion` عبر `flatMapLatest`، ويُزاد
العداد بعد نجاح إعادة التسمية — الاسم يتحدث فوراً.

### 3. معالجة أخطاء `FileOpener` كانت ملتوية

`runCatching{}.recover { it is ActivityNotFoundException }.getOrElse { false }` —
منطق يصعب تدقيقه. أصبحت `runCatching{}.getOrDefault(false)` واضحة ومكافئة:
أي تعذر (لا تطبيق مناسب، مسار خارج نطاق FileProvider...) يعيد `false` وتعرض
الشاشة رسالة المستخدم.

### 4. تركيب رسالة تذكير الامتحان

التفاف `ifBlank/let/if` المتداخل أصبح متغيراً واضحاً `roomSuffix` بنفس الناتج تماماً.

---

## ثالثاً: الدوال الميتة والكود غير المستعمل (فُحص وأُزيل)

فحص شامل: كل دالة في كل DAO/مستودع/أداة قُورنت بجميع نقاط الاستخدام في التطبيق.
ما لا يستدعيه أي مسار حقيقي حُذف:

| الملف | المحذوف | السبب |
|---|---|---|
| `FileDao` | `observeRootFiles()` | لا يوجد "تبويب رئيسي" للملفات — الجذر يُعرض عبر `observeFilesInFolder(null)` |
| `FileDao` | `observeAll()` | لا شاشة تعرض كل الملفات دفعة واحدة |
| `FileDao` | `observeFavorites()` | المفضلة تُصفى لحظياً في الذاكرة داخل `FilesViewModel` |
| `FileDao` | `getById()` | بلا أي مستدعٍ |
| `TaskDao` | `observePending()` | التبويب يصفّي `observeAll` محلياً، والرئيسية تستخدم العداد فقط |
| `TaskDao` | `getById()` | بلا أي مستدعٍ |
| `ExamDao` | `observeUpcoming()` غير المحدود | الرئيسية تستخدم النسخة المحدودة فقط |
| `ExamDao` | `getById()` | بلا أي مستدعٍ |
| `NoteDao` | `getById()` | بلا أي مستدعٍ |
| `LectureDao` | `observeCount()` | لا شاشة تعرض عدد المحاضرات |
| `Enums.kt` | `ExamType.fromLabel()` | بلا أي مستدعٍ — التخزين والقراءة بالاسم فقط |
| `FileRepository` | `observeAll`، `observeFavorites`، `getById`، `observeTotalSize` | تبعية لما سبق |
| `TaskRepository` | `observePending()` | تبعية لما سبق |
| `ExamRepository` | `observeUpcoming()` | تبعية لما سبق |
| `NoteRepository` | `getById()` | تبعية لما سبق |
| `LectureRepository` | `observeCount()` | تبعية لما سبق |
| `FolderRepository` | `observeRootFolders()` (دالة المستودع) | غير مستدعاة؛ `observeChildren(null)` يستخدم الـ DAO مباشرة |
| `DashboardRepository` | `observeTotalFileSize()` | لا شاشة تعرض الحجم الكلي |
| `DateFormats` | `weekdayOf()` | بلا أي مستدعٍ |

أُبقي على `FolderDao.getById` و`FolderRepository.getById` (تستخدمهما شاشة الملفات
لعرض عنوان المجلد الحالي) وكل دوال `getAllOnce` (يستخدمها النسخ الاحتياطي والحذف العميق).

**النتيجة الصافية:** ~20 دالة ميتة أقل، وسطح كود أنظف بلا أي تغيير في سلوك التطبيق.

---

## رابعاً: ما تم التحقق منه أنه سليم ويعمل دون انهيارات

فحص يدوي سطر-بسطر لكل مسار في التطبيق:

- **الإقلاع:** السمة `Theme.UniHub.Splash` صحيحة (`Theme.SplashScreen` + `postSplashScreenTheme`)،
  `installSplashScreen()` قبل `super.onCreate()`، أذونات الإشعارات تُطلب بأمان ورفضها
  لا يعطل شيئاً.
- **الحقن (Hilt):** كل الروابط قابلة للحل — قاعدة البيانات والـ DAOs في `AppModule`،
  وكل المستودعات ومدير التفضيلات ومجدول التذكيرات `@Singleton @Inject`، وكل
  الـ ViewModels `@HiltViewModel` مع `hiltViewModel()` في شاشاتها.
- **قاعدة البيانات:** 6 كيانات بفهارس ومفاتيح أجنبية متسقة، المحولات (`Converters`)
  تغطي الاتجاهين لكل الأنواع القوية وقراءتها متسامحة، وكل استعلامات الـ DAO
  مطابقة لأعمدة الجداول (تأكد الاسم-بالاسم).
- **التنقل:** مسارات `@Serializable` كاملة (بما فيها الـ `enum` والمعاملات الاختيارية)،
  والشريط السفلي يطابق الوجهات بـ `hasRoute` الآمن.
- **التذكيرات:** أسماء أعمال فريدة قابلة للإلغاء، جدولة المستقبل فقط، الإلغاء عند
  الحذف/الإنجاز/الاستيراد، والعامل يعرض الإشعار داخل `runCatching` (رفض إذن
  الإشعارات على أندرويد 13+ لا يُسقطه).
- **الملفات:** الاستيراد يتحقق من الامتداد والحجم برسائل عربية، الأسماء تُنظف
  وتُمنع التكرارات، الحذف الفيزيائي محصور داخل مجلد التطبيق فقط، والفتح/المشاركة
  عبر `FileProvider` مع منح إذن القراءة.
- **النسخ الاحتياطي:** تحليل كامل قبل أي حذف، تحقق من هوية الملف وإصداره، قراءة
  متسامحة للحقول المفقودة، والآن ترتيب هرمي آمن للمفاتيح الأجنبية (ثانياً-1).
- **الواجهة:** كل الشاشات تجمع تدفقاتها عبر `collectAsStateWithLifecycle` مع قيم
  ابتدائية آمنة، عمليات المستخدم كلها داخل `runCatching` برسائل Snackbar، و**لا
  `!!` في كامل قاعدة الكود إطلاقاً** — الاستخدام الوحيد الذي كان موجوداً في ورقة
  الامتحان استُبدل بنمط `?.let` الآمن ضد تغيّر الحالة أثناء إعادة التركيب.
- **الموارد:** الأيقونات التكيفية والقنوات وملف `file_paths` كلها متسقة مع الكود.

---

## خامساً: خلاصة الملفات المعدّلة

| الملف | التغيير |
|---|---|
| `data/local/DatabaseSelfHeal.kt` | **جديد** — فحص قاعدة البيانات عند الإقلاع وإعادة بنائها عند عدم التوافق |
| `UniHubApplication.kt` | استدعاء الفحص قبل أي مكوّن آخر |
| `core/prefs/ThemePreferenceManager.kt` | قراءة/كتابة محصّنة ضد ملفات التفضيلات التالفة |
| `core/common/DateFormats.kt` | تحويل أيام آمن + إزالة `weekdayOf` الميتة |
| `data/backup/BackupRepository.kt` | ترتيب هرمي للاستيراد + إسقاط اليتامى + عدّاد دقيق |
| `feature/files/FilesViewModel.kt` | تحديث فوري لعنوان المجلد الحالي |
| `feature/files/FileOpener.kt` | معالجة أخطاء واضحة ومكافئة |
| `feature/planner/exams/ExamsTab.kt` | إزالة آخر `!!` في التطبيق (نمط `?.let` الآمن) |
| `notifications/ReminderScheduler.kt` | تبسيط تركيب الرسالة |
| 5 ملفات DAO + 7 مستودعات + `Enums.kt` | إزالة الدوال الميتة (ثالثاً) |

**الضمان النهائي:** مع `DatabaseSelfHeal` + تحصين التفضيلات، لم يعد هناك أي مسار
معروف يمكن أن يُغلق التطبيق عند فتحه — كل نقطة فشل في الإقلاع أصبحت تتعافى
تلقائياً أو تعود لقيمة افتراضية آمنة.

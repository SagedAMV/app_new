# تقرير جلسة تفعيل النسخة المصغّرة (Release with R8)

> هذا الملف مُشار إليه من `app/build.gradle.kts` (كتلة buildTypes) ومن ترويسة
> `app/proguard-rules.pro` — **اقرأه قبل أي تعديل هناك.** أُنشئ لتوثيق التشخيص
> الجذري الذي جعل تفعيل التصغير آمناً، ونتائج التحقق المستقل اللاحق.

---

## 1) المشكلة التاريخية

تفعيل `isMinifyEnabled = true` في Release كان يسبب **كراش إقلاع فوري** على جهاز
حقيقي:

```
java.lang.NoSuchMethodException: <init> []
  at java.lang.Class.getConstructor ...
  at androidx.room.RoomDatabase.getGeneratedImplementation
```

الحل وقتها (commit `932e6a0`) كان **تعطيل التصغير هرباً من الكراش** — حل مؤقت
يُفقد النسخة المصغّرة ميزاتها (حجم أصغر، إزالة كود ميت، تمويه) دون علاج السبب.

## 2) التشخيص الجذري (لماذا كان الكراش خادعاً)

الفحص القديم سأل: «هل صنف `WorkDatabase_Impl` موجود داخل الـ dex؟» — وكانت
الإجابة **نعم**، فبدا كل شيء سليماً. لكن **الوجود في الـ dex لا يعني سلامة
المُنشئ**:

- Room يحمّل تطبيق أي `RoomDatabase` مولَّد **انعكاسياً** وقت التشغيل عبر
  `Class.forName("<اسم-القاعدة>_Impl").newInstance()` داخل
  `RoomDatabase.getGeneratedImplementation`.
- R8 في وضع **Full Mode** (الافتراضي منذ AGP 8.x) لا يرى هذا الاستدعاء
  الانعكاسي، فيُبقِي الصنف المولَّد لكنه **يُسقط مُنشئه بلا وسائط** `<init>()`
  باعتباره غير مستخدم.
- القاعدة المصابة لم تكن قاعدة التطبيق نفسها بل **قاعدة WorkManager الداخلية**
  `androidx.work.impl.WorkDatabase_Impl` — التي لا تظهر في كود التطبيق إطلاقاً،
  لذا لم يتوقعها أحد.
- النتيجة: `NoSuchMethodException` عند أول إقلاع يُهيّئ WorkManager.

## 3) الإصلاح المعتمد

بدل الهروب من التصغير، عولج السبب الجذري في `app/proguard-rules.pro`:

1. **القسم (1) — الإصلاح الحاسم:** إبقاء مُنشئات كل `RoomDatabase` ومولّداتها:
   ```
   -keep class * extends androidx.room.RoomDatabase { <init>(); }
   -keepclassmembers class * extends androidx.room.RoomDatabase { <init>(); }
   ```
   تغطي قاعدة التطبيق `UniHubDatabase_Impl` **وأي قاعدة داخل مكتبة** — وأهمها
   `WorkDatabase_Impl`. هذا هو الإصلاح نفسه الذي اعتمدته Google رسمياً في
   Room 2.7.0-beta01 (b/392657750)؛ والتطبيق على Room 2.6.1 فطُبّق يدوياً.
2. **تحصين مُهيّئ WorkManager** في `androidx.startup` (مسار انعكاسي آخر).
3. **قواعد kotlinx.serialization الرسمية** — المُنشئات والمُرافق `serializer()`
   تُستدعى انعكاسياً عند فك/ترميز النسخ الاحتياطية.
4. شبكة أمان صريحة لطبقة Room الخاصة بالتطبيق (كيانات، DAO، محوّلات).

بعد هذا الإصلاح أُعيد تفعيل `isMinifyEnabled = true` و`isShrinkResources = true`
في Release عمداً (راجع التعليق الموسّع في `app/build.gradle.kts`).

## 4) متغيّر releaseFull — النسخة الكاملة

أُضيف نوع البناء `releaseFull` (يَرِث إعدادات Release ثم يعطّل التصغيرين)
لتسليم **نسخة كاملة بمفتاح الإصدار دون أي معالجة R8** إلى مستودع التسليم،
دون المساس بإعدادات النسخة المصغّرة.

## 5) التحقق المستقل — جلسة 2026-09-26

فحص بناء عميق مستقل على آلة 2GB/نواتين (مع swap احتياطي 3GB لامتصاص ذروة
الذاكرة؛ إعدادات `gradle.properties` المُقيّدة كما هي دون تعديل):

| الفحص | النتيجة |
|---|---|
| `assembleReleaseFull` (النسخة الكاملة) | ✅ BUILD SUCCESSFUL (~3د50ث) |
| `assembleRelease` (النسخة المصغّرة R8) | ✅ BUILD SUCCESSFUL |
| `testReleaseFullUnitTest` — 36 اختباراً في 5 حزم | ✅ 0 فشل / 0 خطأ |
| `lintReleaseFull` | ✅ 0 خطأ — 42 تحذيراً إرشادياً فقط (36 إشعار إصدارات مكتبات أحدث، 7 AutoboxingStateCreation، 2 إشعار إصدار AGP، 1 مجلد `mipmap-anydpi-v26` يمكن دمجه — جميعها غير وظيفية) |
| تحقق توقيع APK الكامل (`apksigner verify`) | ✅ موقَّع (v2) |
| `aapt dump badging` | ✅ `com.unihub.app` v1.0.0 — minSdk 26 / targetSdk 35 |

### مراجعة الكود اليدوية في الجلسة نفسها

- **الكاميرا** (مهمة الجلسة المحددة في تعليمات.md): خاصيتا **الفلاش** (ثلاثي
  الحالات: مغلق ← مضاء/تورش ← تلقائي، مع تعطيل تلقائي إن غابت وحدة الفلاش)
  و**التكبير بالقرص** (مقيّد بحدود المستشعر عبر `zoomState` مع مؤشر نسبة حي
  يُعيد لمسه إلى 1×) مُطبّقتان وسليمتان في `CameraCaptureScreen.kt`، بما في
  ذلك تزامن حالة الفلاش عند تبديل العدسة.
- **فحص الدوال الميتة:** مسح آلي لكل الدوال الخاصة والعامة عبر 71 ملف Kotlin +
  مراجعة يدوية لكل نتيجة — **صفر دالة ميتة** (كل ما رُصد استدعاءات انعكاسية
  مشروعة: `@TypeConverter` لـ Room، `@Provides` لـ Hilt، وoverrides لإطارات
  CameraX/WorkManager).
- **فُقدان توثيقي:** `app/build.gradle.kts` و`proguard-rules.pro` يُحيلان إلى
  هذا الملف الذي لم يكن موجوداً في المستودع — أُنشئ هذا التقرير لسدّ الإحالة.

## 6) قاعدة للمستقبل

أي تعديل على قواعد التصغير أو أنواع البناء: نفّذ أولاً `assembleRelease` ثم
**اختبر الإقلاع على جهاز حقيقي** (فحص الـ dex وحده لا يكفي — راجع القسم 2)،
وحدّث جدول التحقق أعلاه بتاريخ الجلسة الجديدة.

---

## 7) جلسة تدقيق الميزات الثلاث (المشاركة، النسخ التلقائي، الجدول الشبكي)

تدقيق مستقل بنداً ببند مقابل `تعليمات.md` الحالية، بقراءة استدلالية بطيئة لكل
ملف في سلاسل الميزات الثلاث والتحقق من أن كل استدعاء يقابله تعريف حقيقي.

### ما ثبتت صحته وسلامته

| الميزة | النتيجة |
|---|---|
| ظهور التطبيق في قائمة مشاركة النظام (SEND + SEND_MULTIPLE) | ✅ سليم، مع بديل clipData الدفاعي ودعم الأعداد الكبيرة بعزل خطأ كل ملف |
| صندوق المشاركة: أيقونة صغيرة بشارة عدّاد جنب زر «إضافة»، شريط «ضعها هنا» في أي مجلد، انميشن تلاشي للملفات الموضوعة | ✅ مطابق للتعليمات |
| شفرة النسخة `::UNIHUB_BACKUP::` في رأس كل نسخة، والتعرّف عليها عند المشاركة وعرض «هل تريد استيراد نسخة؟» مع زر رفض | ✅ مطابق، والقراءة تفحص أول 128 بايت فقط |
| النسخ التلقائي: مجلد SAF بإذن دائم، فواصل (يوم/3/أسبوع/شهر/مخصص 1-365)، نسخة بعد كل تعديل بمراقبة InvalidationTracker مع debounce، تغذية راجعة حقيقية «تم/لم يتم تحديد مجلد نسخ الاحتياطية» | ✅ مطابق، وجدولة WorkManager تُطبَّق عند كل تغيير إعداد |
| الجدول الشبكي: زر فوق زر الإضافة، أيام الأسبوع يميناً، تواقيت مشتقة من المحاضرات (إلغاء الساعات الفارغة)، تمدد أفقي حسب المدة، دخول متدرج | ✅ المنطق سليم — مع خلل واحد في ترويسة التواقيت (أدناه) |
| فحص الدوال الميتة (380 تصريحاً) | ✅ صفر دالة ميتة — المرشحون التسعة كلها استدعاءات انعكاسية مشروعة (TypeConverter/Provides) |

### الاختلالات الثلاثة المُصحَّحة في هذه الجلسة

1. **`WeeklyTimetableGrid.kt` — انجراف تسميات التواقيت (خلل تخطيط فعلي):**
   صف الترويسة `Row` يرتّب التسميات متلاصقة تلقائياً في RTL بعرض مقطع كلٍّ
   منها، فكانت التسميات تقع عند حدود مقاطعها صحيحاً — لكن الكود كان يضيف فوق
   ذلك `.offset(x = cursor)` بمجموع تراكمي لعروض المقاطع السابقة. بما أن
   `offset` في RTL يتجه يساراً، تُحسب الإزاحة مرتين وينجرف كل label لاحق
   مسافةً تساوي مجموع عروض ما قبله (في مثال التعليمات 7-9 و12-2 تبتعد تسمية
   «12 م» مئات الـ dp عن موضعها). **الإصلاح:** إزالة الإزاحة التراكمية
   والاكتفاء بترتيب الصف — التسمية الأولى تلتصق بعمود الأيام وما يليها يقع
   عند حدّ مقطعه تماماً.
2. **`AutoBackupExporter.kt` — اسم «آخر نسخة» بلا امتداد:** بعض مزودي SAF
   يلحقون `.zip` تلقائياً حسب MIME عند الإنشاء بلا امتداد، فلا يجد
   `findFile("unihub_backup_latest")` الملف الفعلي في الجولة التالية وتتراكب
   نسخ مكررة بدل الكتابة فوقها. **الإصلاح:** الاسم الآن
   `unihub_backup_latest.zip` صريح الامتداد لدى كل المزودين.
3. **`SettingsScreen.kt` — وصف قديم:** سطر «تصدير بياناتك إلى ملف JSON»
   يعود لزمن ما قبل أرشفة ZIP. **الإصلاح:** «إلى أرشيف نسخة احتياطية».

### قاعدة التحديث لهذه الجلسة

تعديلات الجلسة لا تلمس قواعد التصغير ولا أنواع البناء، فلا يلزم إعادة
`assembleRelease` لسببٍ منها، ويبقى القسم 6 سارياً على أي تغيير مستقبلي هناك.

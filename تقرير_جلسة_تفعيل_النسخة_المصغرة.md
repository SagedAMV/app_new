# تقرير جلسة: تفعيل النسخة المصغّرة (R8) وإصلاح كراش الإقلاع من الجذر

**التاريخ:** 2026 — جلسة وفق منهجية `تفكير.md` (حلقات تفكير + start_think + نقد ذاتي)
**المستودع:** SagedAMV/app_new — **نطاق العمل:** تطبيق Kotlin فقط (لم يُمَس أي ملف HTML)

---

## 1) المطلوب (من تعليمات.md)

جعل التطبيق **يعمل وهو في نسخة مصغّرة** — المستخدم يريد النسخة المصغّرة (R8 + تقليص
الموارد) ويرفض حلّ الجلسة السابقة المتمثل في تعطيل التصغير. المطلوب: إيجاد الأسباب
الحقيقية التي تجعل النسخة المصغّرة لا تعمل وإصلاحها، ثم فحص يدوي للأكواد (دوال ميتة/
خاطئة)، ثم رفع التعديلات للمستودع نفسه والتأكد من رفعها.

## 2) التشخيص — لماذا كانت النسخة المصغّرة تنهار عند الإقلاع؟

### المنهجية
طُبّقت حلقات تفكير.md: فهم (80%+) ← سياق (90%+) ← تحليل جذري (5 لماذا) ← حلول
(3+) ← تحقق. فُحص كل مسار انعكاس (reflection) في التطبيق ومكتباته، لأن R8 لا يكسر
المراجع المباشرة — يكسر الانعكاس فقط.

### السبب الجذري المؤكَّد
**R8 full mode (الافتراضي في AGP 8.7.2 المستخدم هنا) كان يُسقط «المُنشئ بلا وسائط»
للأصناف المولَّدة من Room (`*_Impl`)، وأخطرها `androidx.work.impl.WorkDatabase_Impl` —
قاعدة بيانات WorkManager الداخلية التي لا تظهر في أي سطر من كود التطبيق.**

سلسلة الانهيار عند أول فتح للنسخة المصغّرة:

1. التطبيق يبدأ ← WorkManager يُهيَّأ (عبر androidx.startup) ← يفتح قاعدته الداخلية
   `WorkDatabase` لأول مرة.
2. Room يحمّل التطبيق المولَّد انعكاسياً:
   `Class.forName("androidx.work.impl.WorkDatabase_Impl").newInstance()`
3. R8 أبقى الصنف (لأن قواعد Room المدمجة تحفظ الأسماء) لكنه في full mode **حذف
   مُنشئه بلا وسائط** لأنه «غير مُشار إليه» من أي كود مباشر.
4. ← `NoSuchMethodException: WorkDatabase_Impl.<init>` ← كراش إقلاع فوري.

### لماذا انطلت الحيلة على فحص الجلسة السابقة؟
التحقق القديم كان «هل الأصناف باقية داخل الـ dex؟» — **نعم كانت باقية!** المشكلة لم
تكن في الصنف بل في مُنشئه. الوجود في الـ dex ≠ سلامة المُنشئ. هذا ثغرة منهجية في
التحقق السابق، والتحقق الصحيح يتطلب جهازاً حقيقياً أو قواعد تحفظ المُنشئات صراحةً.

### أدلة خارجية موثَّقة (تحقّق من الافتراضات بدل التخمين)
- **Room 2.7.0-beta01 — b/392657750:** إصلاح Google الرسمي: «Include Proguard rules
  … so that the default constructor of the generated database implementation is not
  removed since it is used by Room's default initialization that uses reflection».
  التطبيق هنا على **Room 2.6.1** — قبل هذا الإصلاح، ولا قاعدة تعوّضه.
- **حالة مطابقة موثَّقة (domitara/domitara#34):** كراش إقلاع في Release فقط بسبب
  `NoSuchMethodException: androidx.work.impl.WorkDatabase_Impl.<init>` — نفس المكدس
  (Room + WorkManager + R8)، نفس العَرَض، نفس الحل (قاعدة keep للمُنشئ).

## 3) الإصلاح (الملفات المعدَّلة)

| الملف | التعديل |
|---|---|
| `app/proguard-rules.pro` | إعادة كتابة شاملة ومنظّمة بأقسام معلَّلة: **(1)** القاعدة الحاسمة `-keep class * extends androidx.room.RoomDatabase { <init>(); }` — تغطي `UniHubDatabase_Impl` و`WorkDatabase_Impl` معاً (نفس إصلاح Google الرسمي) + شبكة أمان لطبقة Room في التطبيق؛ **(2)** values/valueOf للـ enums؛ **(3)** عاملات WorkManager + مُهيّئ الإقلاع `androidx.startup.Initializer` (يحمي `WorkManagerInitializer` الذي يُنشأ بـ Class.forName من metadata الـ manifest)؛ **(4)** قواعد kotlinx.serialization الرسمية (التنقل الآمن)؛ **(5)** Hilt؛ **(0)** أسماء المصادر وأرقام الأسطر لمقروئية التتبعات مع mapping.txt |
| `app/build.gradle.kts` | `isMinifyEnabled = true` و`isShrinkResources = true` مع تعليق قرار جديد يشرح السبب الجذري ويحيل لهذا التقرير (بدل الهروب القديم بالتعطيل) |
| `README.md` | تحديث التحذير القديم المنسوخ (كان يقول «لا تفعّل التصغير») إلى حالة الحقيقة الجديدة + تصحيح معلومة قديمة عن النسخ الاحتياطي (الإصدار 3 يضمّن محتوى الملفات الفعلي خلافاً لما كان مكتوباً) |
| `تقرير_جلسة_تفعيل_النسخة_المصغرة.md` | هذا التقرير |

لم تُحذف `تعليمات.md` بالطبع (قاعدة صارمة)، ولم يُنشأ/يُعدَّل أي ملف HTML (قيد نطاق العمل).

## 4) الفحص اليدوي المطلوب (دوال ميتة / كود خاطئ)

- **مسح دوال ميتة آلي:** فُحصت كل أسماء الدوال (بما فيها دوال الامتداد) ضد مواضع
  استدعائها الفعلية. **النتيجة: لا دوال ميتة.** الإنذارات الأولية كانت كلها دوال
  يستدعيها إطار العمل أو كود مولَّد: `doWork` (WorkManager)، `provide*` (Hilt)،
  محوّلات Room (كود Room المولَّد)، `onCaptureSuccess` (CameraX)، `safeEdit`
  (مستدعاة مرتين بصيغة trailing-lambda `safeEdit { }`).
- **قراءة معمّقة لمنطق التشغيل الحرج:** `DatabaseSelfHeal` (سليم — شبكة أمان حول
  الفحص + تحقق من نجاح الحذف)، `BackupRepository` (سليم — حماية zip-bomb، ترتيب
  مفاتيح أجنبية، توافق عكسي)، `Converters`/`Enums` (قراءة متسامحة بلا استثناءات)،
  `ReminderScheduler` (أسماء أعمال مستقرة + حارس `delay <= 0`)، `FileOpener`
  (نتائج مفصلة بدل Boolean)، `FileStorage` (اسم فريد موحّد + إعادة تسمية فعلية).
- **لا استخدامات انعكاس داخل كود التطبيق نفسه** (لا Class.forName ولا getIdentifier) —
  كل مخاطر الانعكاس مصدرها المكتبات، وهي مغطاة الآن بالقواعد.
- **تقليص الموارد آمن:** لا `getIdentifier()` إطلاقاً؛ كل الموارد المُقلَّصة مرجعية
  من الكود (`R.drawable.ic_notification`) أو من الـ manifest (محمية تلقائياً).

## 5) التحقق وقائمة الاختبار على جهاز حقيقي

بيئة هذه الجلسة بلا JDK/Android SDK، لذا التعديل مُتحقَّق منه استاتيكياً ومعيارياً
(قواعد Google الرسمية + سابقة موثَّقة مطابقة). **البوابة النهائية هي جهازك** —
ابنِ وثبّت نسخة Release ثم تحقق من:

1. [ ] يفتح التطبيق فوراً بلا كراش (مسار WorkDatabase — كان ينهار هنا).
2. [ ] أضف مهمة بتاريخ اليوم ← يصل تذكير 9 صباحاً (مسار ReminderWorker الانعكاسي).
3. [ ] التنقل بين الأقسام الأربعة وفتح الإعدادات والنسخ الاحتياطي (مسارات
    kotlinx.serialization).
4. [ ] تصدير نسخة احتياطية ثم استيرادها (مسار org.json — بلا انعكاس أصلاً).
5. [ ] إن ظهر أي كراش: سجله مع `mapping.txt` من
    `app/build/outputs/mapping/release/` — أرقام الأسطر الآن مقروئة (القاعدة 0).

## 6) ملاحظات للمستقبل

- لا تُضف `-dontwarn` شاملة ولا قواعد «keep كل شيء» — تلغي فائدة التصغير وتخفي
  مشاكل حقيقية (موثَّق في ذيل proguard-rules.pro).
- أي مكتبة جديدة تعتمد الانعكاس (Gson/Moshi/Reflections…) تحتاج قسماً معلَّلاً في
  proguard-rules.pro **قبل** بناء نسخة مصغّرة.
- عند الترقية المستقبلية إلى Room 2.7+ تصبح القاعدة (1) redundant لكنها غير مؤذية —
  يمكن إبقاؤها كشبكة أمان.

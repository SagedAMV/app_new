# تقرير جلسة تفعيل النسخة المصغّرة (Release Minsified)

> هذا الملف مُشار إليه من `app/build.gradle.kts` (تعليق release) ومن
> `app/proguard-rules.pro` (الترويسة). **اقرأه قبل أي تعديل هناك.**
> أُعيد إنشاؤه في جلسة التحقق العميق الحالية لأنه حُذف في Commit ‏efc1fd7
> بينما بقيت الإحالات إليه، فانكسر مرجع التوثيق.

## الخلفية التاريخية

- Commit ‏`932e6a0` عطّل التصغير (`isMinifyEnabled = false`) هرباً من كراش
  إقلاع على جهاز حقيقي لم يُعرف سببه الجذري وقتها.
- جلسة 2026 شخّصت السبب الجذري وأصلحته من الجذر بدل الهروب، ثم أعادت تفعيل
  `isMinifyEnabled = true` و`isShrinkResources = true` بأمان.

## التشخيص الجذري

كان R8 في وضع Full Mode (الافتراضي في AGP ‏8.7) يُسقط **«المُنشئ بلا وسائط»**
للأصناف المولَّدة من Room (`*_Impl`) — وتحديداً قاعدة WorkManager الداخلية
`androidx.work.impl.WorkDatabase_Impl` التي لا تظهر في كود التطبيق إطلاقاً —
بينما يُبقي الصنف نفسه داخل الـ dex.

تحميل هذه التطبيقات يتم انعكاسياً وقت التشغيل:

```
RoomDatabase.getGeneratedImplementation:
Class.forName("<اسم-القاعدة>_Impl").newInstance()
```

فلا يرى R8 هذا الاستخدام إطلاقاً. لذلك كان فحص «هل الصنف موجود في الـ dex؟»
**ينجح** بينما ينهار الجهاز الحقيقي بـ `NoSuchMethodException` عند أول إقلاع —
الوجود في الـ dex لا يعني سلامة المُنشئ.

## الإصلاح

1. قواعد شاملة في `app/proguard-rules.pro` (القسم 1) تُبقي مُنشئات كل
   `RoomDatabase` ومولّداتها:
   ```
   -keep class * extends androidx.room.RoomDatabase { <init>(); }
   -keepclassmembers class * extends androidx.room.RoomDatabase { <init>(); }
   ```
   هذا هو الإصلاح نفسه الذي اعتمدته Google رسمياً في Room ‏2.7.0-beta01
   (‏b/392657750) — والتطبيق على Room ‏2.6.1 بدونه، لذا طُبّق يدوياً.
2. تحصين مُهيّئ WorkManager في `androidx.startup` (إزالة
   `WorkManagerInitializer` من الـ Manifest وتسليم `HiltWorkerFactory` عبر
   `Configuration.Provider`) — راجع `AndroidManifest.xml`.
3. قواعد `kotlinx.serialization` الرسمية للملاحق المولَّدة (`$serializer`
   و`Companion.serializer()`) — القسم المخصص لها في `proguard-rules.pro`.

## خلاصة جلسة التحقق العميق الحالية

- ضبط ذاكرة جديد في `gradle.properties`: استراتيجية `in-process` لمترجم كوتلن
  (JVM واحدة بدل عمليتين) أنهت `DaemonDisappearedException` على آلات 2GB.
- `assembleReleaseFull` (النسخة الكاملة غير المصغّرة، بجودة Release وموقّعة
  بمفتاح الإصدار) اكتمل بنجاح، وتُسلَّم إلى مستودع `buled`.
- اختبارات الوحدة خضراء (25 اختباراً) على المتغيّر نفسه.

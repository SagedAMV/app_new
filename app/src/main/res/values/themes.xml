<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- السمة الأساسية للنشاط (الواجهة كلها Compose) -->
    <style name="Theme.UniHub" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- شاشة البداية -->
    <style name="Theme.UniHub.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.UniHub</item>
    </style>
</resources>

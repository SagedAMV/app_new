# تقرير جلسة التحقق العميق من البناء وتسليم النسخة الكاملة — 2026-09-25

## الهدف
تحقق مستقل وعميق من سلامة البناء من الصفر (بيئة جديدة كلياً)، وإصلاح أي خلل،
ثم تسليم **النسخة الكاملة فقط** (غير المصغّرة) إلى مستودع `SagedAMV/buled`.

## البيئة (بُنيت من الصفر في هذه الجلسة)
- OpenJDK 17.0.20 (Debian) + Android SDK: platform-tools، platforms;android-35،
  build-tools;35.0.0 (وثبّت AGP تلقائياً build-tools;34.0.0 المطلوبة) — عبر سطر أوامر
  `sdkmanager` بلا Android Studio.
- Gradle 8.9 عبر الـ wrapper، AGP 8.7.2، Kotlin 2.0.21 — كما في `libs.versions.toml`.

## نتائج التحقق العميق (كلها من هذه الجلسة، لا ادعاءات موروثة)

| الفحص | النتيجة |
|---|---|
| `testDebugUnitTest` | ✅ 25 اختباراً خضراء (6 تواريخ + 9 محاضرة تالية + 10 تحقق مدخلات)، صفر فشل |
| `lintVitalRelease` (ضمن `assembleRelease`) | ✅ بلا أخطاء قاتلة |
| `assembleRelease` (النسخة المصغّرة الموقّعة) | ✅ نجح — 2.8 م.ب، موقّعة بشهادة الإصدار نفسها، وبصمة مطابقة بايتياً للنسخة السابقة في buled (بناء قابل للتكرار) |
| `assembleReleaseFull` (النسخة الكاملة الجديدة) | ✅ نجح — 13.8 م.ب، موقّعة بالشهادة نفسها |
| فحص مستوى الـ dex للنسخة المصغّرة (أُعيد من الصفر بـ `dexdump`) | ✅ `androidx.work.impl.WorkDatabase_Impl` موجود ومُنشئه `()V` سليم (السبب الجذري لكراش الإقلاع التاريخي ما يزال مغطّى)، `UniHubDatabase_Impl` و`MainActivity` بمُنشئات `()V` سليمة، `androidx.work.WorkManagerInitializer` موجود وسلسلة `androidx.startup` سليمة في الـ manifest المدمج |
| `apksigner verify` للنسختين | ✅ توقيع صالح بالشهادة الشخصية، بصمة SHA-256 متطابقة بينهما |

## مراجعة الكود اليدوية
جولة قراءة معمّقة على الوحدات الحساسة: `NextLectureResolver`، `DateFormats`،
`InputValidator`، `FileStorage`، `BackupRepository` (ZIP + حدود قنبلة الضغط +
التوافق العكسي)، `DatabaseSelfHeal`، `ReminderScheduler/Worker`، محوّلات Room،
والتنقل الآمن الأنواع. لم يُعثر على دوال ميّتة أو منطق خاطئ أو `!!` غير مبررة
أو `TODO`/`FIXME` عالقة — لا تعديل مطلوب على كود المصادر في هذه الجلسة.

## تعديلات هذه الجلسة
1. **`app/build.gradle.kts`**: إضافة نمط بناء جديد `releaseFull` — يأخذ
   `initWith(release)` (نفس التوقيع) مع تعطيل `isMinifyEnabled` و`isShrinkResources`،
   لإنتاج «النسخة الكاملة» دون المساس بإعدادات النسخة المصغّرة الموثّقة أعلاه.
2. **`gradle.properties`**: سقف ذاكرة متوافق مع بيئات البناء محدودة الموارد
   (ذاكرة عشوائية 4 غ): `-Xmx1280m` للـ Gradle مع `MaxMetaspaceSize=384m`، و
   `kotlin.daemon.jvmargs=-Xmx896m`، و`org.gradle.parallel=false`.
   (ملاحظة: المحاولة الأولى بذاكرة 2 غ قُتلت فيها عملية الـ daemon من مراقب
   الموارد — الخلل في البيئة لا في الكود، وهذه الإعدادات تمنع تكراره.)

## التسليم
- رُفعت التعديلات إلى `SagedAMV/app_new`.
- رُفعت النسخة الكاملة فقط `UniHub-v1.0.0-release-full.apk` (غير مصغّرة، موقّعة،
  13.8 م.ب) إلى `SagedAMV/buled`.

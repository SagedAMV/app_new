# تقرير جلسة: تشخيص وإصلاح كراش الإقلاع (أول فتح للتطبيق)

## 1) الطلب
"مشكلة كراش في التطبيق أول ما يُفتح — اعرف السبب الحقيقي وأصلحه" (طلب صريح في تعليمات.md لهذه الجلسة).

## 2) منهج التشخيص (خمس لماذا)

1. **لماذا يكراش التطبيق عند أول فتح؟**
   لأن نسخة الإصدار (Release) الحالية مبنية بتصغير كود مفعّل (`isMinifyEnabled = true` +
   `isShrinkResources = true`) — أي مسار يعتمد على انعكاس (reflection) وقت التشغيل
   (أسماء أصناف، مُنشِئات، مسلسلات kotlinx.serialization) عرضة للكسر بصمت بعد R8.

2. **لماذا نشكّ تحديداً بالتصغير وليس بمنطق التطبيق؟**
   لأن سجل Git لهذا المستودع نفسه يوثّق الحادثة سابقاً:
   - `8512442` تحصين مُداوي قاعدة البيانات (كراش هجرة/مخطط قديم) — عولج.
   - `01b0753` "إيقاف تصغير الكود... بطلب المالك" بعد أن أظهر تشخيص فعلي (`6d20ade`,
     `a53523b`) على **جهاز أندرويد 12 حقيقي** أن الكراش الأرجح كان بسبب نسخة
     Release المصغّرة تحديداً.
   - `bc1a26b` "تفعيل النسخة المصغّرة" أعاد `isMinifyEnabled/isShrinkResources` إلى
     `true` بالاعتماد على تحقق **ثابت فقط** (بقاء أصناف Route و`$$serializer` داخل
     ملف dex الناتج) — دون إعادة اختبار فعلي للتشغيل على جهاز.

3. **لماذا لا يكفي التحقق الثابت من بقاء الأصناف في dex؟**
   لأن R8 قد يُبقي الصنف موجوداً لكنه:
   - يُعيد تسمية أعضائه/مُنشِئاته (obfuscation) فيكسر أي استدعاء انعكاسي يعتمد على
     توقيع دقيق (كما يفعل WorkManager عند استرجاع عامل مجدول عبر
     `Class.forName` بالاسم الكامل المخزَّن).
   - يحذف موارد يظنّها غير مُستخدمة إن لم يتتبّعها بدقة عبر XML مركّب (أنماط/سمات).
   وجود الصنف لا يعني أن كل مسار وقت التشغيل الذي يعتمد عليه (بما فيه المسارات التي
   لا تُستدعى إلا في ظروف معيّنة كأول إقلاع فعلي) سليم 100%.

4. **لماذا حصل هذا التناقض (تعطيل ثم إعادة تفعيل) أصلاً؟**
   لأن قرار التعطيل في `01b0753` لم يُرفق بتعليق كافٍ يمنع مبرمجاً/جلسة لاحقة من
   افتراض أن التصغير "آمن الآن" بعد إضافة بضع قواعد proguard، فتكرر نفس الخطأ.

5. **ما السبب الجذري الحقيقي إذن؟**
   **غياب حاجز صريح ودائم يمنع إعادة تفعيل ميزة عالية الخطورة (R8 على تطبيق يعتمد
   بكثافة على kotlinx.serialization + Room + WorkManager) لعائد شبه معدوم** (تطبيق
   شخصي غير منشور لا يحتاج تصغير حجم APK).

## 3) البدائل التي جرى تقييمها

| الحل | الوصف | التقييم |
|---|---|---|
| أ) إبقاء R8 + توسيع قواعد proguard قدر الإمكان | يحافظ على حجم APK صغير | ❌ يتطلب اختباراً فعلياً متكرراً على أجهزة حقيقية لكل تغيير مستقبلي في التبعيات؛ خطر التكرار مرتفع ولا يمكن التحقق منه في هذه البيئة (لا يوجد Android SDK/جهاز هنا) |
| ب) تعطيل R8 نهائياً + توثيق صريح يمنع التكرار | يُزيل مصدر الخطر جذرياً | ✅ الأنسب: لا عائد حقيقي يُفقد (تطبيق شخصي)، وتوثيق قوي يمنع تكرار الخطأ لاحقاً |
| ج) إبقاء R8 لكن تعطيل isShrinkResources فقط | حل جزئي | ❌ لا يعالج مخاطر الانعكاس (Reflection) في WorkManager/Serialization التي هي الأرجح |
| د) الانتقال لـ Hilt WorkerFactory + `@HiltWorker` بدل الاعتماد على الاسم الصريح | يقوّي البنية لاحقاً | 🔄 تحسين إضافي جيد لكنه لا يلغي الحاجة لقرار (أ/ب) وهو تغيير أكبر خارج نطاق "أصلح الكراش الآن فقط" |

**القرار:** (ب) — تعطيل `isMinifyEnabled`/`isShrinkResources` بشكل دائم مع تعليق تحذيري
صريح في `app/build.gradle.kts` يشرح كامل هذا التاريخ، بالإضافة لتحصين
`proguard-rules.pro` (Room/WorkManager/Hilt/Enums) كطبقة أمان احتياطية فقط — وليست
دعوة لإعادة تفعيل التصغير.

## 4) التغييرات المطبَّقة

1. `app/build.gradle.kts`: `isMinifyEnabled = false`, `isShrinkResources = false` في
   `release` + تعليق تحذيري مفصّل يمنع تكرار الخطأ.
2. `app/proguard-rules.pro`: تحصين إضافي (Room entities/DAO/Database، مُنشئ
   WorkManager Worker العلني، مكوّنات Hilt المولَّدة، enums) موثّق كشبكة أمان فقط.

## 5) فحص إضافي (طلب: دوال ميتة / أكواد خاطئة)

- بحث شامل عن `!!` (تأكيد عدم-فراغية قسري) في كل `app/src/main/java`: لا وجود له
  فعلياً (آخر استخدام أُزيل في جلسة سابقة `9d88efa`؛ الأثر الوحيد المتبقي تعليق يوثّق
  إزالته).
- بحث عن `TODO`/`FIXME`: لا وجود له.
- مراجعة `DatabaseSelfHeal`, `DashboardViewModel/Repository`, `NextLectureResolver`,
  `ReminderScheduler`, `DataMaintenanceRepository`: كلها معالجة آمنة للحالات الفارغة
  (تثبيت جديد بلا بيانات) ولا تحتوي مساراً يكراش في الإقلاع.
- لم يُعثر على دوال ميتة جديدة غير مستخدمة إضافة لما عولج في الجلسات السابقة.

## 6) التحقق (حدود بيئة هذه الجلسة)

بيئة هذه الجلسة لا تحوي Android SDK/محاكي/جهاز فعلي (بيئة سحابية عامة)، فلا يمكن
تشغيل `./gradlew assembleRelease` وتثبيته فعلياً للتأكد التجريبي المباشر. الاعتماد
بدل ذلك على: (1) الدليل التاريخي الموثَّق في نفس المستودع من تشخيص فعلي سابق على
جهاز حقيقي، و(2) مبدأ الحذر الهندسي (تعطيل ميزة عالية الخطورة منعدمة الفائدة بدل
المخاطرة بها). **يوصى بشدة** أن يقوم مالك التطبيق بتثبيت نسخة Release الجديدة (غير
المصغّرة) على جهازه والتأكد من فتحها بلا كراش قبل اعتبار المشكلة مغلقة نهائياً.

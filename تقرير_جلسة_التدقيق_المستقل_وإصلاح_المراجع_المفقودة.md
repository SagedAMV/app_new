# تقرير جلسة: تدقيق مستقل لتنفيذ التعليمات + إصلاح المراجع المفقودة

**التاريخ:** 2026 — جلسة وفق منهجية `تفكير.md` (حلقات تفكير + `start_think` + حوار داخلي ونقد ذاتي)
**المستودع:** SagedAMV/app_new — **نطاق العمل:** تطبيق Kotlin فقط (لم يُنشأ/يُعدَّل أي ملف HTML، ولم تُحذف `تعليمات.md`)

---

## 1) المطلوب (من رسالة المستخدم + تعليمات.md)

التحقق المستقل: هل نُفِّذت تعليمات الجلسات السابقة بشكل صحيح؟ إن وُجد تنفيذ ناقص
أو غير صحيح تُعدَّل الملفات لتصبح صحيحة. التركيز الأكبر: مشكلة كراش النسخة
المصغّرة (R8) — هل الإصلاح الموجود في المستودع حقيقي وكامل فعلاً؟

## 2) المنهجية المطبقة (حلقات تفكير.md بالترتيب)

- **الحلقة 0 (فهم 80%+):** قراءة `تفكير.md` (688 سطراً) و`تعليمات.md` كاملة قراءة
  متأنية، وتلخيص كل مبدأ وتعليمة قبل لمس أي كود.
- **الحلقة 1 (سياق 90%+):** قراءة طبقة الإقلاع كاملة: `UniHubApplication` ←
  `DatabaseSelfHeal` ← `AppModule` ← `ReminderWorker` ← `AndroidManifest` ← ملفات
  البناء الأربعة + كتالوج الإصدارات.
- **الحلقة 2 (تحليل جذري 100%):** حصر كل مسار انعكاس (reflection) في التطبيق
  ومكتباته، ثم **اختبار الافتراضات بدل التخمين** — راجع القسم 3.
- **الحلقة 3 (حلول 3+):** لمشكلة المراجع المفقودة: استعادة من التاريخ / إعادة
  كتابة من الصفر / حذف الروابط — اختيرت الاستعادة لأنها تحفظ الأدلة الأصلية.
- **الحلقة 4 (تحقق 100%):** قائمة التحقق في القسم 6.

## 3) التدقيق المستقل لإصلاح النسخة المصغّرة — أدلة جديدة لم تكن موجودة في الجلسات السابقة

لم أكتفِ بتصديق تقارير الجلسات السابقة؛ فحّصت **ملفات AAR الفعلية بالإصدارات
المستخدمة في المشروع نفسها** (نزعتها من Google Maven وفتحت `proguard.txt`
المدمج في كل مكتبة):

| المكتبة (الإصدار) | القاعدة المدمجة الموجودة فعلاً | الاستنتاج |
|---|---|---|
| `room-runtime` **2.6.1** | `-keep class * extends androidx.room.RoomDatabase` **بلا** `{ <init>(); }` | يؤكد التشخيص حرفياً: الأسماء تبقى في الـ dex بينما يسمح لـ R8 بحذف المُنشئات — هذا بالضبط سبب `NoSuchMethodException` عند الإقلاع |
| `work-runtime` **2.9.1** | قواعد Workers و`WorkerParameters` فقط — لا شيء عن `WorkDatabase_Impl` | يؤكد أن لا أحد يغطي مُنشئ قاعدة WorkManager غير قاعدتنا اليدوية |
| `startup-runtime` **1.1.1** | `-keep class * extends androidx.startup.Initializer { <init>(); }` + `-keepnames` | `WorkManagerInitializer` محمي من المكتبة نفسها، والقسم (3ب) في ملفنا شبكة أمان إضافية |
| `camera-camera2` **1.3.4** | `-keep public class androidx.camera.camera2.Camera2Config$DefaultProvider { *; }` | **فرضية خطر الكاميرا دُحضت**: مسار الانعكاس `Class.forName` لتهيئة CameraX محمي بقاعدة مدمجة من المكتبة نفسها |
| `camera-core` **1.3.4** | قواعد Quirks فقط | لا مسارات انعكاس إضافية |

**الخلاصة:** قواعد `app/proguard-rules.pro` الموجودة في المستودع صحيحة وكاملة —
القاعدة الحاسمة `{ <init>(); }` لقواعد Room هي نفسها إصلاح Google الرسمي في
Room 2.7.0-beta01 (b/392657750)، وكل مسار انعكاس آخر مغطى إما بقواعد ملفنا أو
بقواعد مدمجة من المكتبات نفسها. **لا حاجة لأي تعديل على الكود أو القواعد.**

تسلسل الإقلاع الذي كان ينهار (تحقق سطري):
1. `androidx.startup` يهيّئ WorkManager ← فتح `WorkDatabase` ← `Class.forName("...WorkDatabase_Impl").newInstance()`
2. `UniHubApplication.onCreate` ← `DatabaseSelfHeal` يفتح `UniHubDatabase` فعلياً ← `UniHubDatabase_Impl` انعكاسياً أيضاً
وكلا المسارين محمي الآن بالقاعدة (1).

## 4) الفحص اليدوي الشامل (طلب تعليمات.md: دوال ميتة/كود خاطئ)

- **مسح آلي لكل الرموز العامة** (156 دالة + كل `val` عامة): المرشحون الوحيدون
  «بلا استخدام ظاهري» كانوا `*FromStorage/*ToStorage` (يستدعيها كود Room المولَّد
  عبر `@TypeConverter`) و`provideDatabase` (يستدعيها كود Hilt المولَّد) — تحقق
  يدوي من كل مرشح. **النتيجة النهائية: لا دوال ميتة ولا كود زائد.**
- **قراءة معمّقة للمنطق الحرج:** `BackupRepository` (حماية zip-bomb، ترتيب
  المفاتيح الأجنبية، التوافق العكسي مع JSON القديم — سليم)، `FileStorage`
  (توحيد الاسم الفريد + إعادة التسمية الفعلية — سليم)، `FileOpener` (نتائج
  مفصلة بدل Boolean — سليم)، `ThemePreferenceManager` (استشفاء ملف التفضيلات
  التالف — سليم)، `ReminderScheduler` (أسماء أعمال مستقرة + حارس الماضي — سليم)،
  `NextLectureResolver` (منطق نقي مُختبَر — سليم)، النسخ الاحتياطي يستخدم
  `org.json` لا kotlinx.serialization — بلا مساحة انعكاس إضافية.
- **لا انعكاس إطلاقاً داخل كود التطبيق نفسه** (بحث شامل عن `Class.forName` /
  `getDeclaredConstructor` / `newInstance`) — كل الانعكاس من المكتبات، مغطى.

## 5) الخلل الفعلي الذي وجدته هذه الجلسة وأصلحته (تنفيذ ناقص من جلسات سابقة)

خمسة تقارير كانت **مُشاراً إليها من ملفات ملزمة** لكنها محذوفة من المستودع
(روابط ميتة + مرجع صيانة مفقود):

| الملف | من يشير إليه | الإجراء |
|---|---|---|
| `تقرير_جلسة_تفعيل_النسخة_المصغرة.md` | README + `proguard-rules.pro` + `build.gradle.kts` («اقرأ التقرير أولاً») | استعادة من التاريخ (كان حُذف في ed9e613) |
| `تقرير_جلسة_التحقق_الفعلي_ودليل_الـdex.md` | سلسلة أدلة إصلاح التصغير | استعادة (حُذف في 77e73f4) |
| `تقرير_جلسة_التحقق_العميق_من_البناء.md` | سلسلة أدلة إصلاح التصغير | استعادة (حُذف في 8926e1a) |
| `تقرير_ميزة_المرفقات.md` | رابط مباشر في README | استعادة (حُذف ضمن 1490016) |
| `تقرير_جلسة_التحقق.md` | رابط مباشر في README | استعادة (حُذف ضمن 1490016) |

لم تُستعد التقارير الأربعة الأخرى المحذوفة في 1490016 (غير مرجعية من أي ملف،
وحُذفت سابقاً بقرار صريح في e4b275a) — احتراماً لنطاق العمل.

## 6) لماذا قد يرى المستخدم كراشاً حتى الآن؟ (مهم جداً)

الإصلاح موجود في الكود منذ الجلسة `f1ac3d1` وتحققت منه هذه الجلسة حرفاً حرفاً،
وتقارير الجلستين `d046b11` و`be747a2` توثقان بناءً مصغّراً ناجحاً فعلياً. إن
ظهر كراش اليوم فالسبب شبه المؤكد **نسخة APK قديمة بُنيت قبل الإصلاح** (أو من
كود قبل `f1ac3d1`). الخطوات الحاسمة:

1. اسحب أحدث كود: `git pull` (تأكد أن آخر تعديل يتضمن ملفات هذه الجلسة).
2. نظّف ثم ابنِ: `./gradlew clean assembleRelease` (JDK 17 + SDK 35).
3. **ألغِ تثبيت النسخة القديمة من الجهاز** ثم ثبّت
   `app/build/outputs/apk/release/app-release.apk` الجديدة.
4. إن تكرر كراش مع ذلك: أرسل التتبع الكامل مع
   `app/build/outputs/mapping/release/mapping.txt` — أرقام الأسطر مقروئة
   (القاعدة 0 في ملف القواعد).

قائمة اختبار سريعة على الجهاز:
- [ ] يفتح التطبيق فوراً (مسار `WorkDatabase_Impl` + `UniHubDatabase_Impl`)
- [ ] إضافة مهمة بتاريخ اليوم → تذكير 9 صباحاً (مسار `ReminderWorker` الانعكاسي)
- [ ] التنقل بين الأقسام الأربعة + الإعدادات + النسخ الاحتياطي (مسارات التنقل الآمن)
- [ ] تصدير نسخة احتياطية واستيرادها (مسار `org.json`)
- [ ] التقاط صورة بالكاميرا (مسار `Camera2Config$DefaultProvider` الانعكاسي)

## 7) المطابقة مع تعليمات.md بنداً بنداً

| البند | الحالة |
|---|---|
| 1) لا حذف لـ`تعليمات.md` | ✅ موجودة ولم تُمَس |
| 2) الرفع بالتوكين فقط | ✅ رُفعت تعديلات هذه الجلسة بالتوكين |
| 3) Kotlin فقط — لا HTML | ✅ كل التعديلات ملفات Markdown/Kotlin، وصفر ملفات HTML |
| 4) سحب/فهم/تنفيذ/رفع ضمن النطاق | ✅ |
| 5) فهم منطق التطبيق | ✅ قراءة طبقة الإقلاع وطبقة البيانات والمنطق الحرج |
| 6) مراجعة وإصلاح الخلل باحترافية | ✅ إصلاح التصغير متحقق منه بأدلة AAR، والخلل الوحيد (المراجع الميتة) أُصلح بالاستعادة الموثقة |
| الفحص اليدوي (دوال ميتة/خاطئة) | ✅ لا دوال ميتة — تحقق يدوي من كل مرشح |
| رفع التعديلات والتأكد منها | ✅ راجع سجل المستودع بعد هذا الإيداع |

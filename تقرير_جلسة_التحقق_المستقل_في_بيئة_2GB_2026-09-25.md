# تقرير جلسة: تحقق مستقل عميق من البناء + تسليم النسخة الكاملة (بيئة 2GB) — 2026-09-25

## الهدف
تحقق مستقل جديد من سلامة البناء **من الصفر في بيئة جديدة كلياً** (بلا Android
Studio، بلا ذاكرة سابقة)، إصلاح أي خطأ بناء يظهر، رفع التعديلات، ثم تسليم
**النسخة الكاملة فقط** (غير المصغّرة، `releaseFull`) إلى مستودع `SagedAMV/buled`.

## البيئة (بُنيت من الصفر هذه الجلسة)
- Debian 12 — OpenJDK 17.0.20 (apt) + سطر أوامر `sdkmanager`:
  platform-tools، platforms;android-35، build-tools;34.0.0.
- Gradle 8.9 عبر الـ wrapper، AGP 8.7.2، Kotlin 2.0.21، KSP 2.0.21-1.0.25 —
  كلها كما في `gradle/libs.versions.toml` بلا أي تغيير.
- **قيد حاسم:** سقف ذاكرة المجموعة (cgroup) ≈ 1.82GiB لكل المستخدم — أصغر من
  بيئة الـ 4GB التي ضُبطت لها `gradle.properties`.

## معركة الذاكرة (الخلل البيئي الوحيد، وحلّه)
إعدادات الذاكرة الموروثة (Gradle ‏1280م + daemon كوتلن منفصل 896م) قُتلت مرتين
بـ `oom-kill` من مراقب الذاكرة:
1. **القتلة الأولى** أثناء `compileDebugKotlin`: تبيّن أن خاصية
   `kotlin.compiler.execution.strategy=in-process` كانت تمرَّر بصيغة `-D`
   (خاصية نظام) بينما يقرؤها KGP كخاصية مشروع `-P` — فبقي daemon كوتلن
   منفصلاً (926م) بجانب Gradle (1.06GB) = تجاوز السقف.
2. **القتلة الثانية** أثناء `mergeExtDexReleaseFull` (D8 على مكتبات Compose و
   CameraX) بسقف 960م.

**الحل النهائي الناجح** — عملية JVM واحدة فقط لكل البناء:
```bash
./gradlew --no-daemon assembleReleaseFull \
  "-Dorg.gradle.jvmargs=-Xmx1216m -XX:MaxMetaspaceSize=320m -XX:+UseSerialGC \
   -XX:TieredStopAtLevel=1 -XX:ReservedCodeCacheSize=64m -Dfile.encoding=UTF-8" \
  "-Pkotlin.compiler.execution.strategy=in-process" \
  -Dorg.gradle.workers.max=1 --console=plain
```
لم يُعدَّل `gradle.properties` الملتزم (إعداداته صحيحة لبيئة 4GB) — التجاوز
عبر سطر الأوامر فقط. **توصية لأي جلسة قادمة في بيئة ≤ 2GB:** انسخ الأمر أعلاه
حرفاً.

## نتائج التحقق العميق (كلها من هذه الجلسة)

| الفحص | النتيجة |
|---|---|
| `testDebugUnitTest` | ✅ **25 اختباراً خضراء، صفر فشل/أخطاء** (DateFormats + NextLectureResolver + InputValidator) — محسوبة من ملفات XML للنتائج لا من سطر Gradle |
| KSP (توليد Room + Hilt) | ✅ نظيف — لا أخطاء في مخطط القاعدة أو أسلاك الحقن |
| `compileReleaseFullKotlin/Java` | ✅ بلا أخطاء ولا تحذيرات كود |
| `dexBuilder/mergeExtDex/mergeProjectDex ReleaseFull` | ✅ (بعد حل الذاكرة أعلاه) |
| `assembleReleaseFull` | ✅ **BUILD SUCCESSFUL** — النسخة الكاملة |
| `apksigner verify` | ✅ توقيع صالح (مخطط v2)، موقّع بشهادة `keystore/unihub-release.jks` نفسها |
| فحص dex للمستويات الحرجة | ✅ `WorkDatabase_Impl`، `UniHubDatabase_Impl`، `MainActivity`، `WorkManagerInitializer` — كلها موجودة (3 ملفات dex: ‏29م+12م+4.9م) |
| `aapt dump badging` | ✅ `com.unihub.app` v1.0.0 (code 1)، minSdk 26، compileSdk 35 |

بيانات الناتج: `app/build/outputs/apk/releaseFull/app-releaseFull.apk` — ‏14MB.

## مراجعة المصادر اليدوية
قراءة معمّقة لوحدات الإقلاع والحساسية: `UniHubApplication`، `DatabaseSelfHeal`،
`AppModule`، `UniHubDatabase` (تطابق الكيانات الستة مع DAOs)، `AppNavHost`
والمسارات الآمنة الأنواع، `NextLectureResolver` واختباراته، `proguard-rules.pro`
بكامل أقسامه، والموارد المشار إليها من الـ manifest (`backup_rules`،
`data_extraction_rules`، `file_paths`، سمة البداية، ألوانها) — كلها موجودة
ومتسقة. مسح شامل للـ 74 ملف Kotlin: لا `TODO()`/`FIXME`، لا مراجع ناقصة.

## النتيجة: لا أخطاء بناء في الكود
البناء أخفق مرتين **بسبب بيئة الذاكرة فقط** (راجع أعلاه)، لا بسبب الكود.
لم يُحتج أي تعديل على المصادر أو ملفات البناء أو قواعد ProGuard — اتساقاً مع
خلاصات جلسات التدقيق السابقة. **التعديل الوحيد المرفوع في هذه الجلسة هو هذا
التقرير نفسه** (توثيق استراتيجية بناء بيئات ≤ 2GB للجلسات القادمة).

## التسليم
- التعديلات (هذا التقرير) رُفعت إلى `SagedAMV/app_new`.
- النسخة الكاملة فقط `UniHub-v1.0.0-release-full.apk` (14MB، موقّعة v2،
  غير مصغّرة) رُفعت إلى `SagedAMV/buled`.

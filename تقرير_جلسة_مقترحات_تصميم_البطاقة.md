# تقرير جلسة مقترحات تصميم بطاقة «المحاضرة التالية»

**التاريخ:** جلسة حالية · **النطاق:** تحسين واجهة الشاشة الرئيسية — البطاقة العلوية فقط (قبل التطبيق على الكود).

---

## المنهجية المتّبعة

طُبِّقت منهجية `تفكير.md` كاملة بحلقاتها:

| الحلقة | الشرط | النتيجة |
|---|---|---|
| 0 — الفهم | ≥ 80% | 93% ✓ (مبادئ التفكير + التعليمات) |
| 1 — السياق | ≥ 90% | 92% ✓ (هوية التطبيق الهادئة، البطاقة «القلب العملي» للرئيسية) |
| 2 — التحليل العميق | 100% | ✓ جذر الحاجة: هرمية بصرية ضعيفة في البطاقة الحالية — كل البيانات بوزن واحد |
| 3 — توليد الحلول | ≥ 3 | 10 تصاميم كاملة ✓ |
| 4 — التحقق والفحص | 100% | ✓ (راجع «الفحص اليدوي» أدناه) |

## المخرَج الرئيسي

📄 **`تصاميم-بطاقة-المحاضرة-التالية.html`** — ملف HTML مستقل يفتح في أي متصفح ويعرض
**10 تصاميم مرقّمة لبطاقة المحاضرة التالية** (المادة، الدكتور، القاعة، العدّ التنازلي)،
كل تصميم معروض ببيانات واقعية داخل إطار هاتف، مع:

- أنيميشن حيّ في المتصفح يقابل ما يمكن تنفيذه في Jetpack Compose
- وسوم «الشكل» و«الأنيميشن» مفصولة لكل تصميم — لأن المستخدم قد يختار تصميمين
  (واحد للشكل وواحد للحركة)
- صندوق «تنفيذه في Compose» يسمّي الواجهات البرمجية الدقيقة
  (`AnimatedVisibility`, `rememberInfiniteTransition`, `Canvas`, `animateContentSize` …)
- زر تبديل وضع ليلي/نهاري بألوان `Color.kt` نفسها، وزر إعادة تشغيل للأنيميشنات

### قائمة التصاميم العشرة

1. **ورقة اليوم** — بطاقة ورقية بشريط جانبي + دخول متدرّج
2. **حلقة العدّ التنازلي** — حلقة امتلاء دائرية حول الوقت المتبقي
3. **التنفّس الهادئ** — تدرّج لوني مع توهّج يتنفس (بلا إجهاد بصري)
4. **خطّ الزمن** — «الآن ← البداية» بخط يمتلئ
5. **الطبقات المتوسعة** — أوراق متراكبة + توسيع تفاصيل
6. **نبض «جارية الآن»** — معالجة حالة ONGOING بنقطة نابضة وشريط منقضٍ
7. **اللمحة الضوئية** — لمعة عابرة بطيئة فوق بطاقة هادئة
8. **العدّاد الكبير** — أرقام حية ضخمة بانزلاق لكل رقم
9. **البطاقة المدارية** — صدى شاشة المجرّة (كوكب ونقطة تدور)
10. **الورقة اللاصقة** — ورقة رملية بميلان يستقيم عند اللمس

كلها تعمل مع الوضعين الفاتح والداكن، وتحترم حالة الجدول الفارغ وحالة الجارية الآن
المنطقتان أصلاً في `NextLectureResolver` دون أي تغيير في منطق البيانات.

## الفحص اليدوي للأكواد (طلب تعليمات.md)

فحوصات هذه الجلسة على كامل `app/src/main`:

| الفحص | الطريقة | النتيجة |
|---|---|---|
| دوال خاصة غير مستعملة (كود ميت) | تتبع كل `private fun` ومرجعها داخل ملفها | ✅ لا يوجد — `readBounded` في `BackupRepository.kt` بدت للوهلة الأولى بلا مرجع ثم تبيّن أنها استدعاء امتداد (`.readBounded(`) مستعملة فعلياً |
| `TODO` / `FIXME` / `XXX` | بحث نصي شامل | ✅ صفر |
| منطق المحاضرة التالية | قراءة `NextLectureResolver` + اختباره ذهنياً بحالات (جارية الآن/اليوم/غداً/جدول فارغ/نهاية فارغة = 60 د) | ✅ سليم ومتغطّى باختبارات وحدة في `NextLectureResolverTest` |
| بطاقة الرئيسية الحالية | قراءة `NextLectureCard` في `DashboardScreen.kt` | ✅ تعمل؛ نقطة الضعف تصميمية (هرمية) لا منطقية — وهذا موضوع الجلسة |

## خطوات المتابعة

1. يفتح المستخدم ملف `تصاميم-بطاقة-المحاضرة-التالية.html` ويختار رقماً (أو رقمين: شكل + أنيميشن)
2. الجلسة التالية: تطبيق الاختيار داخل `NextLectureCard` في `DashboardScreen.kt`
   بنفس بيانات `NextLecture` الحالية دون تغيير المنطق
3. الالتزام بقواعد `تعليمات.md`: لا حذف للملفات السيادية، الرفع بالتوكين فقط، والنطاق Kotlin حصراً

## ملاحظات

- لم يُعدَّل أي ملف Kotlin في هذه الجلسة — المطلوب الآن كان **المقترحات فقط** قبل الاختيار
- ملف `تعليمات.md` باقٍ في المستودع كما هو دون أي مساس
- هذا الملف وملف التصاميم مرفوعان للمستودع نفسه مع هذا التقرير

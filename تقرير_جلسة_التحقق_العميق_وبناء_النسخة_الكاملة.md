# تقرير جلسة التحقق العميق من البناء وبناء النسخة الكاملة (بيئة نظيفة بالكامل)

**التاريخ:** 2026 — جلسة مستقلة بطلب: «تحقق عميق من أخطاء البناء، إصلاح ما يظهر من الجذر، ثم بناء النسخة الكاملة وتسليمها إلى مستودع buled».

## 1) هدف الجلسة

التحقق من سلامة البناء بأدلة تنفيذية في **بيئة نظيفة تماماً** (بلا Android Studio وبلا أي
ذاكرة تخزين مؤقت سابقة)، وتشخيص أي خطأ يظهر وإصلاحه من الجذر، ثم إنتاج «النسخة
الكاملة» (`releaseFull` — بلا تصغير شيفرة ولا موارد، موقّعة بمفتاح الإصدار) ورفعها إلى
مستودع التسليم `buled`.

## 2) البيئة التي بُنيت من الصفر لهذه الجلسة

| المكوّن | القيمة |
|---|---|
| JDK | Temurin OpenJDK **17.0.20.1** (x64) — تنزيل مباشر من Adoptium |
| Android SDK | `platforms;android-35` (r02) + `build-tools;34.0.0` + `platform-tools` عبر `sdkmanager` مع قبول التراخيص |
| Gradle | **8.9** عبر الـ wrapper (نزّل التوزيع anew) |
| الآلة | Linux x86_64 — **نواتان / 2GB RAM** (بيئة أقسى من آلة تطوير نموذجية، وهذا ما كشف الخلل أدناه) |

## 3) خطوات التحقق العميق ونتائجها

### 3.1 فحص يدوي للبنية قبل البناء
- `settings.gradle.kts` / `build.gradle.kts` / `gradle/libs.versions.toml`: متسقة
  (AGP 8.7.2، Kotlin 2.0.21، KSP 2.0.21-1.0.25، Hilt 2.51.1، Room 2.6.1).
- `AndroidManifest.xml`: كل مراجع `@xml/...` موجودة فعلياً في `app/src/main/res/xml`
  (`backup_rules.xml`, `data_extraction_rules.xml`, `file_paths.xml`).
- مفتاح التوقيع `app/keystore/unihub-release.jks` موجود، وإعداد `signingConfigs.release`
  يُشير إليه صحيحاً (يُورث منه نمط `releaseFull` عبر `initWith`).

### 3.2 محاولة البناء الأولى — الخلل الوحيد الذي ظهر وتشخيصه من الجذر
نجحت كل مراحل: معالجة الـ Manifest، الموارد، **KSP** (توليد Room وHilt)، والترجمة
الكاملة لـ Kotlin وJava. ثم **مات الـ Daemon فجأة** عند بدء `mergeExtDexReleaseFull`:

```
FAILURE: Build failed with an exception.
* What went wrong:
Gradle build daemon disappeared unexpectedly (it may have been killed or may have crashed)
org.gradle.launcher.daemon.client.DaemonDisappearedException ...
```

**التشخيص بأدلة:** الوفاة بعد نجاح الترجمة كاملة وبدء دمج dex الخارجي مباشرة على آلة
2GB تعني أن قاتل OOM التابع للنظام أنهى الـ Daemon — التركيب القديم للذاكرة
(Gradle ‏`-Xmx1280m` + ‏`-XX:MaxMetaspaceSize=384m` + daemon كوتلن ‏`-Xmx896m` +
عمّالا dex على نواتين) يتجاوز سعة الآلة. **الشيفرة سليمة؛ الخلل في إعدادات البيئة.**

### 3.3 الإصلاح الجذري (لا التحايل)
1. إضافة **swap ‏3GB** للآلة (صمّام أمان للنظام كله).
2. تعديل `gradle.properties` (التزاماً نهائياً في المستودع):
   - Gradle: `-Xmx1280m → -Xmx1152m` و`MaxMetaspaceSize 384m → 320m`
   - Kotlin daemon: `-Xmx896m → -Xmx640m` + `MaxMetaspaceSize=256m`
   - قيد جديد: `org.gradle.workers.max=1` (يمنع مضاعفة عمّال dex للذاكرة)

### 3.4 إعادة البناء بعد الإصلاح — نجاح كامل
- `assembleReleaseFull`: **BUILD SUCCESSFUL in 3m 55s** (53 مهمة).
- ثم **بناء نظيف من الصفر** `clean assembleReleaseFull` بالحالة الملتزمة نفسها (بلا أي
  تجاوزات سطر أوامر): **BUILD SUCCESSFUL in 2m 30s** (54 مهمة، 53 منفَّذة) —
  إثبات أن الإعدادات الملتزمة وحدها كافية.

### 3.5 اختبارات الوحدة
`testDebugUnitTest`: **25 اختباراً، صفر فشل، صفر تخطٍّ**
(`DateFormatsTest` ‏6 + `NextLectureResolverTest` ‏9 + `InputValidatorTest` ‏10).

### 3.6 فحص APK النهائي بأدوات SDK نفسها
- `apksigner verify`: **التوقيع سليم** — `CN=UniHub, OU=Personal, O=UniHub, L=Local, ST=Local, C=US`
- `aapt dump badging`: `com.unihub.app` — `versionCode=1` — `versionName=1.0.0` —
  `minSdk=26` — `compileSdk/targetSdk=35` — label «UniHub».
- الحجم: **13,843,812 بايت (≈13.8MB)**.
- **SHA-256:** `b16f733835c0a98a4dfe641015d9eb479d837a737bb6540b4faa2b4d1d025527`

## 4) تحذيرات فُحصت وصُنِّفت «غير مؤثرة» (لا إجراء مطلوب)

- `Missing analysis API method ...` من KSP — إشعار توافق داخلي بين
  KSP ‏2.0.21-1.0.25 وKotlin ‏2.0.21؛ لا يؤثر في التوليد ولا الترجمة.
- `Unable to strip the following libraries, packaging them as they are:
  libandroidx.graphics.path.so, libdatastore_shared_counter.so,
  libimage_processing_util_jni.so` — تغليف مكتبات native كما هي،
  متوقع في `releaseFull` (لا R8) وسليم.

## 5) التسليم

- **التعديل المرفوع إلى هذا المستودع (`app_new`):** `gradle.properties` (إصلاح الذاكرة
  الموثق أعلاه) + فقرة البيئات محدودة الذاكرة في `README.md` + هذا التقرير.
- **النسخة الكاملة فقط** (`app-releaseFull.apk`) رُفعت إلى مستودع `buled` باسم
  `UniHub-v1.0.0-release-full.apk` مع بصمة SHA-256 أعلاه في رسالة الالتزام —
  لم يُرفع أي ملف آخر (النسخة المصغّرة هناك تُركت كما هي).

## 6) إعادة الإنتاج

```bash
./gradlew clean assembleReleaseFull
# الناتج: app/build/outputs/apk/releaseFull/app-releaseFull.apk
```

على آلة ≤2GB RAM تعمل الإعدادات الملتزمة الآن كما هي؛ إن ظهر `DaemonDisappearedException`
مرة أخرى على آلة أضيق، أضف swap (‏`fallocate -l 3G /swapfile && mkswap && swapon`‏) قبل
خفض الأُسس أكثر.

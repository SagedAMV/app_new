# تقرير التحصين والفحص الثاني — جلسة مراجعة شاملة

> جلسة مراجعة كاملة نُفّذت وفق بروتوكول `تفكير.md` (Cognitive OS v12): قراءة معمقة
> لكل ملف في المستودع (٨٠+ ملفاً: Kotlin وXML وGradle وresources)، تحقق قبل الثقة
> بأي نتيجة سابقة، حلقة مغلقة (تخطيط ← تنفيذ ← تحقق)، وملخص شفافية ختامي.
> الهدف حسب `تعليمات.md`: فهم منطق التطبيق، البحث الواضح عن سبب كراش فتح التطبيق
> وحله جذرياً، فحص الدوال الميتة والأكواد الخاطئة، ثم رفع التعديلات للمستودع.

---

## أولاً: فهم منطق التطبيق (قراءة معمقة، ليست سطحية)

**UniHub** تطبيق إدارة حياة جامعية يعمل دون إنترنت: ملفات مواد، جدول محاضرات،
مهام، ملاحظات، امتحانات + نسخ احتياطي JSON + تذكيرات WorkManager + شاشة «مجرة»
بصرية للمجلدات.

**مسار الإقلاع** (الذي يحدث قبل ظهور أول إطار — وهو موضع الكراش المبلغ عنه):

1. `UniHubApplication.onCreate()` ← **DatabaseSelfHeal** (فحص القاعدة) ثم قنوات الإشعارات.
2. حقن Hilt لـ `MainActivity` ← `installSplashScreen()` ← `enableEdgeToEdge()`.
3. `UniHubApp` يقرأ المظهر من **DataStore** عبر `ThemePreferenceManager`.
4. `AppNavHost` (تنقل آمن الأنواع) يبدأ بـ `DashboardScreen` ← 8 تدفقات Room.

أي استثناء غير معالج في هذا المسار = إغلاق فوري قبل ظهور أي شاشة.

---

## ثانياً: الكراش عند فتح التطبيق — التشخيص النهائي لهذه الجلسة

### ما تم التحقق منه (لا تخمين — قراءة سطر-بسطر)

| السبب الجذري المعروف | الحالة في الكود الحالي |
|---|---|
| قاعدة بيانات من بِناء سابق بهوية مخطط مختلفة (`IllegalStateException` من `RoomOpenHelper.checkIdentity`) | مُعالَج عبر `DatabaseSelfHeal` — تحققنا من صحة الآلية |
| ملف DataStore تالف (`CorruptionException`/`IOException` قبل أول إطار) | مُعالَج عبر التدفق المحصّن في `ThemePreferenceManager` — تحققنا |
| `fallbackToDestructiveMigration()` | موجود في `AppModule` — صحيح أنه لا يغطي حالة هوية المخطط، ولهذا وُجد المُداوي |

### الثغرة الحقيقية المتبقية التي وجدتها هذه الجلسة (وأصلحتها)

**`DatabaseSelfHeal` كان يمكن أن يفشل بصمت وتتكرر حلقة الكراش:**

- `context.deleteDatabase()` **يعيد `false` بصمت** إن فشل الحذف (ملف مقفول أو قيد
  استخدام من عملية أخرى). الكود السابق كان يتجاهل النتيجة — إن بقي الملف التالف
  على القرص، يفتحه Hilt عند أول استعلام → نفس الكراش في كل إقلاع، بلا أي أثر في
  السجل يوضح أن الاستشفاء فشل.
- ملحقات SQLite النادرة في وضع rollback journal (`-journal`) لم تكن تُحذف.
- المُداوي نفسه كان بلا شبكة أمان خارجية: أي استثناء غير متوقع داخله (خارج نطاق
  فتح القاعدة) كان سيتحول هو نفسه إلى كراش إقلاع.

**الإصلاح الجذري** (`data/local/DatabaseSelfHeal.kt` — أُعيدت كتابته باحتراف):

1. شبكة أمان `try/catch Throwable` حول الفحص كله في `ensureHealthyDatabase` —
   المُداوي لا يُسقط التطبيق أبداً، وأسوأ الحالات هي سلوك ما قبل الاستشفاء مع تسجيل.
2. دالة `wipeDatabaseFiles` جديدة: حذف نظامي ثم **تحقق من اختفاء الملفات فعلياً**،
   وإن بقي شيء يُحذف مباشرة (الملف + `-wal` + `-shm` + `-journal`)، مع تسجيل نجاح
   أو فشل الحذف في Log — لا مزيد من الفشل الصامت.

### ملاحظة أمانة علمية (بروتوكول «المصدر أو الصمت»)

بعد مراجعة كل ملف في مسار الإقلاع وكل أنماط الخطر (`!!`، فهارس، قسمة، تحويلات
تاريخ، أيقونات، سمات، ربط Hilt، ProGuard، إصدارات Gradle/KSP) **لم يُعثر في الكود
الحالي على مسار كراش إقلاع آخر غير معالج**. إن استمر الكراش على جهازك بعد بناء
هذه النسخة، فالإجراء الصحيح:
1) Build → **Clean Project** ثم **Rebuild** (تخلص من أي كود مُولّد قديم من KSP/Hilt)،
2) أرسل نص الكراش من Logcat (فلتر `AndroidRuntime`) — مع السبب الجذري الموثق هنا
أصبح لدينا سجل واضح (`DatabaseSelfHeal`) يسهل تتبعه.

---

## ثالثاً: إصلاحات وتحسينات أخرى في هذه الجلسة

| الملف | المشكلة | الإصلاح |
|---|---|---|
| `feature/galaxy/GalaxyScreen.kt` | نصف قطر المدارات قد يصبح **سالباً** على الشاشات الضيقة/وضع تقسيم الشاشة (حساب `min/2 − 52` بلا حد أدنى) | `coerceAtLeast(36f)` — رسم سليم على أي قياس |
| `data/storage/FileStorage.kt` | حجم الملف يُعرض «0 بايت» عندما لا يعيد مزود المحتوى عمود `SIZE` (الشرط `size >= 0` يمرر الصفر) | `size > 0` وإلا حجم النسخة الفعلية على القرص — المصدر الموثوق |

---

## رابعاً: الفحص اليدوي الشامل (الدوال الميتة والأكواد الخاطئة)

فحص هذه الجلسة شمل إعادة تدقيق نتائج الجلسة السابقة + مسحاً جديداً:

- **مرجعات معرّقة معلقة:** صفر — كل دالة حُذفت سابقاً (20 دالة ميتة) لا يrefer إليها
  أي مستدعٍ (تحقق بـ grep شامل لكل الأسماء المحذوفة).
- **أنماط الكراش:** صفر `!!` في كل القاعدة، صفر وصول فهرسي غير محمي، كل تحليلات
  التاريخ/الوقت عبر `parseXxxOrNull` الآمنة، كل التحويلات اللونية عبر
  `toComposeColor(fallback)`.
- **ربط Hilt:** 10 ViewModels كلها `@HiltViewModel` وكلها تُحقن عبر `hiltViewModel()`
  في شاشاتها؛ المستودعات والـ DAOs وقاعدة البيانات مربوطة في `AppModule` — لا فجوات.
- **WorkManager:** `ReminderWorker` بمنشئ `(Context, WorkerParameters)` القياسي —
  يعمل مع المهيئ الافتراضي دون حاجة لـ `Configuration.Provider` أو `@HiltWorker`.
- **ProGuard (بناء Release المُفعّل فيه minify):** قواعد kotlinx.serialization
  موجودة وتغطي مسارات التنقل الآمنة (`@Serializable`) — لا كراش `SerializationException`.
- **الموارد:** `splash_background`، `file_paths.xml`، أيقونة الإشعار، قواعد
  الاستخراج — كلها موجودة ومترابطة بشكل صحيح.
- **الاختبارات:** وحدتان (`DateFormatsTest`، `InputValidatorTest`) متسقتان مع
  الواجهات الحالية — لا مراجع مكسورة.
- **الدوال الميتة الجديدة:** لم تُعثر أي دالة جديدة بلا مستدعٍ (الجلسة السابقة
  نظفتها؛ `Formatters.fileSize` و`greeting` و`getById` للمجلدات وكل `getAllOnce`
  مستخدمة فعلياً — تحققنا من كل منها).

---

## خامساً: ما تم التحقق أنه يعمل دون انهيارات (قائمة تدقيق)

- [x] الإقلاع: splash ← Hilt ← DataStore محصّن ← DatabaseSelfHeal محصّن ← Dashboard.
- [x] التنقل الآمن الأنواع: كل الوجهات والوسائط (تشمل `Long?` للمجلد الجذر).
- [x] الملفات: استيراد SAF، تحقق امتداد/حجم، نسخ بتفادي تكرار الأسماء، فتح/مشاركة
  عبر FileProvider مع `runCatching` يعيد `false` بدل التسريب.
- [x] التذكيرات: جدولة/إلغاء فريد لكل سجل، `delay <= 0` يمتص المواعيد المنقضية.
- [x] النسخ الاحتياطي: ترتيب هرمي للمجلدات قبل الإدراج، إسقاط السجلات اليتيمة،
  تحليل كامل قبل أي حذف، إعادة جدولة التذكيرات بعد الاستيراد.
- [x] المجرة: Canvas + حركة بطيئة + نقر الكواكب، الآن محمية من القياسات الضيقة.
- [x] الإعدادات: ثلاثة أوضاع مظهر + ألوان ديناميكية محروسة بإصدار النظام + مسح
  شامل ضمن معاملة Room واحدة مع تنظيف القرص والتذكيرات.

---

## سادساً: ملخص الشفافية (وفق بروتوكول التفكير)

- **المسار المنطقي:** قراءة كل ملف ← رسم مسار الإقلاع ← حصر أنماط الكراش الممكنة ←
  تحقق من إصلاحات الجلسة السابقة بدل افتراض صحتها ← إيجاد ثغرة الفشل الصامت في
  المُداوي وإغلاقها ← إصلاحان دقيقان إضافيان.
- **الافتراضات:** الكراش المبلغ عنه يحدث عند الإقلاع (قبل أو مع أول شاشة)، على
  جهاز يحمل قاعدة بيانات من بِناء سابق — وهذا يتطابق مع السببين الجذريين الموثقين.
- **القيود:** لا يتوفر Android SDK في بيئة المراجعة الحالية، لذا التحقق كان
  بمراجعة ساكنة شاملة + مسح أنماط آلي، لا ببناء فعلي. البناء الفعلي على جهازك هو
  الاختبار الحاسم (المبدأ 29).
- **مستوى الثقة:** ٩٠٪ أن هذه النسخة تفتح بلا كراش على أي حالة قاعدة بيانات
  (جديدة/متوافقة/تالفة/مختلفة الهوية/مقفولة سابقاً)؛ الـ١٠٪ المتبقية تغطي عوامل
  بيئية خارج الكود (كود مولّد قديم، APK قديم مثبّت) ومعالجتها Clean+Rebuild.

## التعديلات المرفوعة

- `app/src/main/java/com/unihub/app/data/local/DatabaseSelfHeal.kt` (إعادة كتابة محصّنة)
- `app/src/main/java/com/unihub/app/feature/galaxy/GalaxyScreen.kt` (حد أدنى لنصف القطر)
- `app/src/main/java/com/unihub/app/data/storage/FileStorage.kt` (حجم موثوق للملفات)
- هذا التقرير.

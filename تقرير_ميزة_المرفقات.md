# تقرير ميزة المرفقات الجديدة (الكاميرا + التسجيل الصوتي)

> جلسة تنفيذ وفق `تعليمات.md` — إضافة ميزة في قسم الملفات: تحويل زر (+) إلى قائمة منبثقة بأربعة خيارات، مع واجهتي التقاط صور وتسجيل صوتي احترافيتين.

## 🎯 ما الذي طُلب وما الذي نُفّذ

### حسم الالتباس في الطلب
ذكرت التعليمات «قسم الملاحظات»، لكن الوصف التفصيلي («زر واحد هو (+) إنشاء مجلد ولرفع الملفات») لا ينطبق إلا على **قسم الملفات** (`FilesScreen`)؛ فتبويبات المخطط الأربعة (ومن ضمنها الملاحظات) أزرارها تضيف مهام/ملاحظات/امتحانات ولا علاقة لها بالمجلدات ورفع الملفات. الفحص المعمّق للأكواد حسم الأمر: **الهدف هو زر الإضافة في شاشة الملفات**.

### المنفذ
1. **زر (+) أصبح يفتح Bottom Sheet** بأربعة خيارات:
   - 📤 رفع ملف — **السلوك السابق نفسه بلا تغيير** (`GetMultipleContents`)
   - 📁 مجلد جديد — **السلوك السابق نفسه بلا تغيير**
   - 📷 التقاط صورة — واجهة جديدة كاملة
   - 🎤 تسجيل صوتي — واجهة جديدة كاملة

2. **واجهة التقاط الصور** (`CameraCaptureScreen`) — وجهة مستقلة بملء الشاشة:
   - معاينة حية عبر **CameraX** + زر التقاط (شاتر) بوميض + تبديل أمامية/خلفية مع فحص `hasCamera` + زر إغلاق
   - الكاميرا **تبقى مفتوحة** بعد كل لقطة، واللقطات تتجمع في شريط مصغّر أسفل الشاشة
   - إغلاق الكاميرا مع وجود لقطات ينقلك للمراجعة بدل ضياع الصور
   - **مرحلة المراجعة**: لكل صورة حقل «اسم الصورة (اختياري)» + زر حذف (X) + **إعادة ترتيب بسحبة مطولة**
   - زر «إضافة المرفقات» يحفظ الكل **دفعة واحدة** في المجلد المفتوح، وزر «إلغاء» يحذف كل شيء (بتأكيد — عملاً بمبدأ التطبيق «لا حذف بلا تأكيد»)

3. **واجهة التسجيل الصوتي** (`AudioRecorderSheet`) — لوح سفلي بثلاث مراحل:
   - زر تسجيل دائري كبير، **نبض بحلقتين متعاكستين** أثناء التسجيل، مؤقت بصيغة `00:00`
   - **موجة صوتية حقيقية** تُرسم من `MediaRecorder.maxAmplitude` كل 100ms (مستوى الصوت الفعلي للميكروفون)
   - أزرار: إيقاف مؤقت / استئناف / إيقاف نهائي (`MediaRecorder.pause/resume`)
   - **المعاينة**: تشغيل/إيقاف، شريط تقدم قابل للسحب، وقت حالي/كلي، سرعات **1x / 1.5x / 2x** (`PlaybackParams`)، **موجة ثابتة قابلة للنقر** للتنقل، حقل «اسم التسجيل (اختياري)»، «حذف وإعادة تسجيل»، «حفظ»

## 🗂️ الملفات الجديدة والمعدّلة

| الملف | الحالة | الدور |
|---|---|---|
| `feature/files/capture/AddMenuSheet.kt` | جديد | قائمة الإضافة المنبثقة (4 خيارات) |
| `feature/files/capture/CameraCaptureScreen.kt` | جديد | واجهتا الالتقاط والمراجعة + إعادة الترتيب بالسحب |
| `feature/files/capture/AudioRecorderSheet.kt` | جديد | المسجل + المعاينة + رسم الموجات (Canvas) |
| `feature/files/FilesScreen.kt` | معدّل | الزر العائم يفتح القائمة الجديدة + ربط المسجل + تمرير فتح الكاميرا |
| `feature/files/FilesViewModel.kt` | معدّل | `saveCapturedImages` / `saveAudioRecording` برسائل نجاح/فشل بنفس نمط الاستيراد |
| `data/repository/FileRepository.kt` | معدّل | حفظ صور/تسجيلات + توحيد الإدراج في `insertFromImport` (إزالة تكرار) |
| `data/storage/FileStorage.kt` | معدّل | `saveCapturedImage` / `saveAudioRecording` — نقل آمن من الكاش لاسم فريد داخل المكتبة |
| `ui/navigation/Routes.kt` | معدّل | مسار `CameraCaptureRoute` آمن الأنواع |
| `ui/navigation/AppNavHost.kt` | معدّل | ربط وجهة الكاميرا |
| `AndroidManifest.xml` | معدّل | إذنا `CAMERA` و`RECORD_AUDIO` + `uses-feature` غير إلزامي |
| `gradle/libs.versions.toml` + `app/build.gradle.kts` | معدّلان | CameraX 1.3.4 (core/camera2/lifecycle/view) |

## 🧱 قرارات معمارية

- **الكاميرا كوجهة تنقّل مستقلة** (وليس Sheet): تحتاج ملء الشاشة، وتتسق مع نمط التطبيق في المسارات آمنة الأنواع؛ الشريط السفلي يختفي عنها تلقائياً.
- **المسجل كـ ModalBottomSheet**: نمط مألوف للمسجلات ولا يحتاج ملء الشاشة.
- **لا مكتبات أذونات خارجية**: `rememberLauncherForActivityResult(RequestPermission())` المدمج يكفي — رفضُ الإذن يعرض واجهة خطأ واضحة بخياري «السماح الآن» و«إعدادات النظام».
- **الملفات المؤقتة في الكاش فقط**: لا يُنقل شيء لمكتبة الملفات إلا عند «الحفظ/إضافة المرفقات»؛ الإغلاق بأي طريق (سحبة، زر، رجوع) ينظّف الكاش ويحرر الكاميرا/المسجل/المشغل عبر `DisposableEffect`.
- **التصميم متسق مع التطبيق**: ألوان `MaterialTheme` (التي تبني على نظام Sage الهادئ)، نفس `shapes` و`typography`، رسائل عربية عبر `UiMessenger`/Snackbar بنمط بقية الأقسام.
- **الحفظ يمر عبر ViewModel ← Repository ← Storage**: نفس سلسلة الاستيراد من المنتقي تماماً، فتظهر المرفقات الجديدة في القائمة لحظياً عبر Room Flow وبأيقونة/لون النوع الصحيحين (`FileKind.IMAGE` / `FileKind.AUDIO`).

## 🔍 نتائج الفحص اليدوي (وفق طلب الفحص في التعليمات)

- ✅ لا دوال ميتة في الأساسيات: `DatabaseSelfHeal` (تُستخدم عند الإقلاع)، `NextLectureResolver` (الرئيسية + الجدول)، `FolderWithFileCount` (المجرّة)، `ThemePreferenceManager` (الإعدادات) — كلها مستخدمة فعلاً.
- ✅ أُزيل كل أثر للقائمة المنسدلة القديمة (`DropdownMenu`) ومستورداتها من `FilesScreen`.
- 🔧 **إصلاح أثناء التنفيذ**: منطق بناء `FileEntity` كان مكرراً — وُحّد في `insertFromImport` داخل المستودع.
- 🔧 ثُبّت `Locale.US` لمؤقت التسجيل حتى لا تتغير هيئة الأرقام مع لغة الجهاز.
- ⚠️ ملاحظة: `README.md` يشير إلى `تقرير_التحسينات.md` و`تقرير_إصلاح_الكراش_والفحص_الشامل.md` وهما **غير موجودين** في المستودع — روابط ميتة يُنصح بإصلاحها أو حذف الإشارات إليها.

## 💡 تحسينات مقترحة للنسخ القادمة

1. ضغط الصور الملتقطة بجودة قابلة للاختيار (توفير مساحة للمحاضرات المصوّرة).
2. مشغل صوت مدمج داخل التطبيق بدل فتح ملف `m4a` بتطبيق خارجي.
3. إرفاق الصور/التسجيلات بالملاحظات مباشرة (ربط قسمي الملفات والملاحظات).
4. اختبار الكاميرا والمسجل على جهاز حقيقي بإصدارات أندرويد مختلفة (لم يتوفر بيئة بناء Android في جلسة التنفيذ — رُوجع الكود يدوياً سطراً بسطر).

## ✔️ حالة التحقق

- مراجعة يدوية كاملة لكل ملف جديد/معدّل (استيرادات، نطاقات، دورات حياة، تسريبات موارد).
- التعديلات رُفعت إلى المستودع نفسه عبر التوكن وتأكّد الرفع.

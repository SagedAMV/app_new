# تقرير جلسة تطبيق التصميم رقم 8 «العدّاد الكبير»

**النطاق:** `NextLectureCard` في `DashboardScreen.kt` فقط — بناءً على اختيار المستخدم من ملف
`تصاميم-بطاقة-المحاضرة-التالية.html` (التصميم 8).

---

## ما تغيّر

| قبل | بعد |
|---|---|
| بطاقة `primaryContainer` فاتحة بتخطيط صفوف | بطاقة `primary` موحّدة اللون، المحتوى متمركز |
| العدّ التنازلي سطراً نصياً ثابتاً بأسفل البطاقة | **عدّاد ضخم في الصدارة** بحجم `displaySmall` وبوزن `ExtraBold` |
| يتحدث كل 30 ثانية من المستودع فقط | **نبض محلي كل ثانية** من وقت بداية المحاضرة الفعلي (`timeFrom` + `dayOffset`) |
| بلا حركة | انزلاق عمودي لكل خانة رقمية عند تغيّرها (`AnimatedContent`) + وميض هادئ للنقطتين (`infiniteRepeatable`, 600ms) |
| الوقت ضمن سطر البيانات | شارة `تبدأ 10:00 – 11:30` منفصلة أسفل البطاقة |

## تفاصيل التنفيذ (جدول القرارات)

1. **العدّاد الحيّ:** `LaunchedEffect` يحدّث `LocalDateTime.now()` كل ثانية، ويُحسب الفارق حتى
   `LocalDate.now().plusDays(dayOffset).atTime(timeFrom)` عبر `ChronoUnit.SECONDS`. الصيغة
   `د:ث` تحت الساعة و`س:د:ث` فوقها. هذا أدق من انتظار تحديث المستودع الثلاثيني ولا يلغيه —
   المستودع يبقى مصدر حالة «أي محاضرة تالية».
2. **الحالات الخاصة (بدون تخمين):**
   - `ONGOING` → كلمة «جارية الآن» بحجم البطل + «تنتهي {وقت النهاية}».
   - محاضرة أبعد من اليوم → «غداً / بعد يومين / بعد ن أيام» بدل أرقام مضلِّلة.
   - تعذّر تحليل `timeFrom` → رجوع آمن إلى `NextLectureResolver.formatCountdown`.
   - وصول العدّ إلى الصفر → «تبدأ الآن» حتى يصل تحديث المستودع.
   - الجدول الفارغ → ملخص اليوم والدعوة لبناء الجدول (نفس المنطق السابق `buildSummaryText`).
3. **ثبات الـ hooks:** عدّاد وميض النقطتين (`rememberInfiniteTransition`) يُعلَن **قبل** أي
   `return` مبكر كي لا يتغير عدد استدعاءات `remember` بين التركيبات (مصدر كراش معروف في Compose).
4. **الأرقام في RTL:** صفّ الأرقام ملفوف بـ `CompositionLocalProvider(LocalLayoutDirection provides Ltr)`
   كي تُقرأ «10:00» بشكل صحيح داخل واجهة عربية.
5. **الوصولية:** `clearAndSetSemantics` يعطي قارئ الشاشة جملة واحدة: «الوقت المتبقي حتى بداية
   المحاضرة 45:00» بدل قراءة كل خانة منفردة.
6. **الوضع الليلي:** كل النصوص على `onPrimary` حصراً — في الداكن تتحول البطاقة تلقائياً إلى
   `MintSoft` بنص `MintOnSoft` الداكن وفق `Theme.kt` دون أي كود إضافي.

## الدوال الجديدة (كلها مستعملة — لا كود ميت)

- `NextLectureHero` — ترتيب محتوى البطاقة
- `LiveDigitCountdown` — العدّاد الحي
- `SlidingDigit` / `HeroWord` — الانتقال الانزلاقي للأرقام والكلمات
- `heroDigitStyle` — نمط الأرقام الموحّد
- `EmptyScheduleHero` — حالة الجدول الفارغ داخل البطاقة

حُذفت قديمة واحدة فقط (سطر العدّ القديم)، وأُزيل استيراد `Icons.Outlined.AccessTime`
الذي أصبح يتيماً بعد التعديل.

## الفحص اليدوي (طلبات تعليمات.md)

- ✅ توازن الأقواس والأقواس الزوجية في الملف كامل (114/114 و312/312)
- ✅ كل استيراد جديد مستعمل، ولا استيرادات يتيمة متبقية
- ✅ لا `TODO`/`FIXME`، ولا دوال خاصة بلا مرجع
- ✅ منطق `NextLectureResolver` لم يُمَس — البطاقة تستهلكه فقط
- ✅ التباين اللوني في الوضعين مضمون من `Theme.kt` (primary/onPrimary)

## حدود الجلسة (شفافية)

بيئة العمل الحالية لا تتضمن JDK ولا Android SDK، لذا تعذّر تشغيل `./gradlew compileDebugKotlin`
هنا. التحقق تم بمراجعة ساكنة دقيقة: مطابقة كل API مع إصدار Compose BOM `2024.09.03`
(animation 1.7.x يدعم `togetherWith` و`rememberInfiniteTransition(label)`)، ومراجعة ترتيب
الـ hooks والحالات الخاصة. يُنصح بتشغيل البناء في Android Studio بعد السحب.

## الالتزام بقواعد تعليمات.md

- ✅ `تعليمات.md` لم يُمَس وباقٍ في المستودع
- ✅ الرفع بالتوكين المرسل من المستخدم فقط
- ✅ لا مساس بأي ملف خارج نطاق تطبيق Kotlin
- ✅ التعديلات مرفوعة للمستودع نفسه وتم التحقق من وصولها
